# Changelog

All notable changes to roughlogic.com are recorded here. The project follows semantic versioning.

## Unreleased

### UX polish

- Brand mark and tab title relabeled: index.html `<title>` from "roughlogic - field math for the trades" to "Rough Logic"; changelog.html `<title>` from "roughlogic - changelog" to "Rough Logic - Changelog"; visible header brand `wordmark` from "roughlogic" to "Rough Logic" (the visible "field math for the trades." tagline span removed); site.webmanifest `name` and `short_name` to "Rough Logic" so the PWA install card matches.

- Section labels on the home grid drop the "Group A" / "Group B" prefix and now read just the category name (`Electrical`, `Plumbing and Gas`, `HVAC`, `Water Damage and Mold Restoration`, `Carpentry and Construction`, `Fire-Ground Engineering`, `Cross-Trade Utilities`, `Knowledge References`). The per-tile group tag mirrors the change. Internal `tool.group` letters (A-H) and the `data-group` attribute stay so URL filtering and CSS hooks are unaffected.

- Search bar now has tool-name autocomplete via a native `<datalist id="tool-suggestions">` populated at boot from the live `TOOLS` registry. Typing matches against every tool name; picking a suggestion that exactly matches a tool name routes the user straight to that tool view (no extra click). Empty queries and partial substrings continue to drive the live home-grid filter exactly as before. Search-input placeholder swapped from "Search 119 tools" (stale) to "Search tools".

### Build progress (v4)

- Step 55 complete (spec-v4.md): Group P - Field, Backcountry, and SAR (utilities 227-232). New calc-field.js with `computePacing` (calibration-based pace length with terrain factor flat/rolling/steep/brush/snow plus a quick-reference table per stride length), `computeBearingConversion` (magnetic <-> true with east-is-least / west-is-best memo, 0-360 normalization), `computeSlopeAvalanche` (rise/run or measured angle, slope %, 30-45 deg avalanche start-zone window flag), `computeBackcountryNeeds` (water L/day from cool/moderate/hot/extreme bands, kcal/day from easy/moderate/hard/extreme exertion factor, trip totals scaled by days and group size), `computeUTM` with full Krueger forward and inverse Lat/Lon <-> UTM (WGS84) - round-trip Denver lat/lon -> UTM zone 13N -> lat/lon verified to 4 decimal places, and `computeSolarTimes` implementing the public NOAA solar-position algorithm (sunrise, sunset, civil/nautical/astronomical twilight, declination, equation of time; verified against Denver summer-solstice yielding ~14.9 hours daylight and equator equinox yielding ~12 hours). Six renderers added; UTM uses a bespoke renderer to handle the bidirectional input mode. Tools registered in TOOLS group P (`Field, Backcountry, and SAR`); `field` added to TRADES; `P` added to GROUPS. 60 new unit tests in test/unit/calc-field.test.js.

- Step 54 complete (spec-v4.md): Group O - Kitchen and Food Service (utilities 222-226). New calc-kitchen.js with `computeRecipeScale` (linear scale by target/original yield with USDA reference-weight conversion to grams when fractional eggs or unusual cup amounts arise; ingredient densities for flour, sugar, butter, milk, oil, honey, rice, oats), `computeYieldEP` (yield % = EP/AP * 100; EP cost per lb = AP cost / yield), `computeCoolingCurve` (FDA Food Code 2022 135 F -> 70 F in <= 2 hr and 70 F -> 41 F in <= 4 hr with container, product type, and ambient adjustment; pass/fail flags), `computePlateCost` (plate cost, suggested menu price = cost / target food cost %, contribution margin, sanity flag), and `computePanConversion` (pan capacity in qt, pans needed, servings per pan from full / two-thirds / half / third / quarter / sixth / ninth pan sizes at 2.5 / 4 / 6 inch depth; cooling-depth warning at depth >= 4 in cross-references utility 224). Five renderers added; recipe scaling and plate cost use bespoke list-based renderers. Tools registered in TOOLS group O (`Kitchen and Food Service`); `kitchen` added to TRADES; `O` added to GROUPS. 50 new unit tests in test/unit/calc-kitchen.test.js.

- Step 53 complete (spec-v4.md): Group N - Stage and Live Production (utilities 216-221). New calc-stage.js with `computeTrussCapacity` (manufacturer-attributed UDL curves for 12-inch box, 16-inch box, 20.5-inch ladder; equivalent-UDL safety factor with 2x point-load rule of thumb; reactions A/B from simple-beam math), `computeTimeAlignment` (speed of sound c = 331.3 + 0.606 * T_C, delay-tower delay in ms, Haas-window 10-30 ms offset), `computeDMX` (per-fixture channel ranges, universe utilization %, conflict and overflow detection, split-recommendation flag), `computeNeutralImbalance` (I_N = sqrt(I_A^2 + I_B^2 + I_C^2 - I_A*I_B - I_B*I_C - I_A*I_C) with harmonic-load warning; companion to v3 utility 128), `computeSPL` (L2 = L1 - 20 log10(d2/d1) with free-field / hemispherical / indoor mode adjustment), and `computeRiggingCheck` (WLL at angle for shackles / slings / span sets / hoists; choker derate 0.75 matches v3 utility 161). Six renderers added; truss-capacity and DMX use bespoke list-based renderers. Tools registered in TOOLS group N (`Stage and Live Production`); `stage` added to TRADES; `N` added to GROUPS. 60 new unit tests in test/unit/calc-stage.test.js. After Steps 53-55: 1802 unit tests passing; lint clean; data:verify clean (66 entries / 56 shards / 10 datasets); home-view payload 36.6% of 100 KB budget.

### Build progress (v4)

- Step 52 complete (spec-v4.md): Group M - Water and Wastewater Operations (utilities 210-215). New calc-water.js with `computePoundsFormula` (lb/day = MGD * mg/L * 8.34, with adjusted product feed at the chemical's purity for chlorine gas, sodium hypochlorite 12.5%, calcium hypochlorite 65%, fluorosilicic acid 23%, alum dry, alum liquid 48.5%, ferric chloride 38%), `computeFilterLoading` (loading rate gpm/ft^2, backwash flow, rapid-sand vs high-rate category), `computeDetentionTime` (volume/flow in min/hr/days with target pass/fail), `computeDilution` (C1V1 = C2V2 single-mode missing-side solve plus serial-mode step series), `computePumpEfficiency` (water HP, brake HP estimate, wire-to-water % with good/ok/degraded category), and `computeSRTandFM` (solids retention time and food-to-microorganism ratio with conventional-activated-sludge range check). Six renderers added. Tools registered in TOOLS group M (`Water and Wastewater Operations`); `water` added to TRADES; `M` added to GROUPS. 60 new unit tests in test/unit/calc-water.test.js.

- Step 51 complete (spec-v4.md): Group L - Agriculture and Forestry (utilities 203-209). New calc-agriculture.js with `computeGPA` (gallons per acre = (5940 * GPM) / (speed * spacing) with inverse for required GPM), `computeTimberCruise` (Doyle (D-4)^2 * (L/16), Scribner public-domain table, International 1/4 inch 0.22 D^2 - 0.71 D), `computeSeedRate` (seeds per acre from row spacing or target population, lbs per acre, optional cost), `computeDrawbarPower` (drawbar HP = pull * speed / 375 with PTO HP estimate by tractive efficiency), `computeUniformity` (Christiansen CU and DU from catch-can readings), `computeBulkDensity` (bulk density, total porosity, compaction flag against texture-class threshold), and `computeCropYield` (bu/acre adjusted to standard moisture for corn/soy/wheat with optional ground-loss percent). Seven renderers added. Tools registered in TOOLS group L (`Agriculture and Forestry`); `agriculture` added to TRADES; `L` added to GROUPS. 70 new unit tests in test/unit/calc-agriculture.test.js including USDA worked examples (Doyle 14 in / 16 ft = 100 BF, Scribner 14 in = 114 BF).

- Step 50 complete (spec-v4.md): Group K - Mechanic, Auto / Marine / Aviation (utilities 195-202). New calc-mechanic.js with `computeWeightBalance` (CG = total moment / total weight against AFM CG and gross limits, FAA AC 91-23A cited by name only), `computePropSlip` (theoretical kt = (RPM/gear) * pitch / 1056; planing 10-15% vs displacement 25-30% category), `computeDisplacementCR` (displacement in^3/L and static compression ratio = (V_cyl + V_TDC) / V_TDC with V_TDC = chamber + gasket + deck - dome), `computeBoltStretch` (clamp load F = (stretch * area * E) / grip across steel/stainless/inconel/titanium/aluminum), `computeDriveshaftCritical` (Euler-Bernoulli first-mode N_crit = (4.7/L^2) * sqrt((EI)/(rho*A)) with 0.65x recommended max), `computeFuelRange` (BTU and kWh from tank * LHV, range from tank * mpg * load factor across gasoline E10/E85, diesel #2, LPG, CNG, jet A), `computeTireGearing` (parses metric 285/75R17 and imperial 33x12.50R17 sizes; rev/mi, effective ratio, recommended axle ratio from candidate set 3.73 / 4.10 / 4.56 / 4.88 / 5.13 / 5.38), and `computeBrakePadLife` (KE = 0.5 m v^2, rotor temp rise, wear-rate by organic/semi-metallic/ceramic). Eight renderers added. Tools registered in TOOLS group K (`Mechanic - Auto, Marine, Aviation`); `mechanic` added to TRADES; `K` added to GROUPS. 82 new unit tests in test/unit/calc-mechanic.test.js including 350 SBC ish 5.7 L sanity, KE-from-mass-and-velocity verification, and tire-size parser round-trip. After Steps 50-52: 1629 unit tests passing; data:verify clean (66 entries / 56 shards / 10 datasets); home-view payload 35.2% of 100 KB.

### Build progress (v4)

- Step 49 complete (spec-v4.md): Group J - Trucking and Logistics (utilities 188 through 194). New calc-trucking.js with `computeDIM` (carrier-divisor DIM weight; UPS / FedEx / USPS / DHL / freight, cited by carrier name only), `computeFreightDensity` (NMFTA density-class bracket Class 50 through 500), `computePalletLoadout` (pallets per trailer by floor area and weight rating, cube-out vs weigh-out flag, 53 ft / 48 ft / 28 ft pup / 40 ft reefer / 20 / 40 ft ocean container), `computeHOS` (FMCSA 49 CFR 395 by section: 11 hr drive, 14 hr window, 70/8 or 60/7 weekly, 30-min break flag), `computeBridgeFormula` (per-axle 20k single, 34k tandem, plus W = 500 * (LN/(N-1) + 12N + 36) across every consecutive group), `computeReeferBurn` (Thermo King / Carrier Transicold manufacturer-attributed GPH benchmarks with hot/cold/moderate ambient factor), and `computeIncoterm` (Incoterms 2020 plain-English decoder cited by ICC name only). Seven renderers added; the Hours-of-Service and Bridge-Formula renderers are bespoke (multi-row inputs); the others use a compact `_simpleRenderer` factory. New TOOLS group J in app.js (`Trucking and Logistics`); `trucking` added to TRADES; `J` added to GROUPS. Two new data shards in scripts/build-data.mjs: `data/trucking/dim-divisors.json` (carriers cited by name only) and `data/trucking/reefer-burn.json` (Thermo King + Carrier Transicold attribution). 71 new unit tests in test/unit/calc-trucking.test.js including bridge-formula violation detection, HOS 30-min break logic, pallet weigh-out vs cube-out, NMFTA density-class boundaries, and DDP / EXW Incoterm allocations. 1415 unit tests passing; data:verify clean (66 entries / 56 shards); home-view payload 33.7% of 100 KB budget.

### Build progress (v3)

- Step 47 complete (spec-v3.md): Group I v3 - Field Notes scratchpad and Crash-Safe Resume (utilities 185, 187). Utility 185: a 280-character textarea per tool view that mounts under the calculator and binds to the URL hash key `k=`. Typing debounces 200 ms then `history.replaceState`s the new hash so the back button is not polluted by every keystroke. Notice under the textarea: "Notes are stored in the URL only. Closing the tab without bookmarking discards them." `routing.js` already parses arbitrary query params via `URLSearchParams`, so `params.k` rehydrates the textarea on a bookmarked load with no parser changes. Utility 187: `renderToolView` now wraps the renderer body and the post-mount support-libs in a try/catch boundary. On uncaught error, log `{ tool, params, error }` to console and mount a `.crash-panel` `role="alert"` reading "This calculator crashed. Reload to retry, or paste your URL into a new tab to recover state. The URL is preserved." The hash is intentionally untouched so the user can recover. New `.scratchpad-region` and `.crash-panel` CSS in styles.css; print stylesheet keeps the scratchpad visible (with a 1px black border) and hides the URL-only notice line. Utility 186 (Send To Other Device) deferred until utility 181 QR is implemented (Step 45). Note: skipping the QR step for now because the bundled MIT QR library is non-trivial and would push the home payload past the budget if not dynamic-imported; will revisit.

- Step 46 complete (spec-v3.md): Group I v3 - Big Buttons mode and High-Contrast theme (utilities 182, 183). theme.js rewritten to support a 3-way theme cycle (`dark` -> `light` -> `high-contrast` -> `dark`) keyed off the existing `rl-theme` localStorage key, plus a Big Buttons mode keyed off a new `rl-bigbuttons` localStorage key (both keys are explicitly blessed by the v3 spec section 1). High-contrast palette: pure black surfaces (`#000000`), pure white text, 2px white borders on every panel, and a high-visibility yellow focus ring (`#ffff00`); WCAG AAA contrast across all text. Big Buttons mode bumps `--touch-min` from 48 px to 64 px, font size 1.125x, input/button heights to 64 px, and tile padding up. Both toggles wired in `<head>` before first paint to avoid flicker. New `<button id="big-buttons-toggle" class="theme-toggle big-buttons-toggle">Big</button>` in the header next to the existing theme toggle; pressed state inverts color. Numpad layout toggle (utility 184) deferred - current `inputmode="decimal"` already gives every number field a numeric soft keyboard on mobile; a separate phone/calculator layout selector adds UI weight without obvious payoff. Will revisit if a user asks. 1344 unit tests still passing; home-view payload 33.0% of 100 KB.

- Step 44 complete (spec-v3.md): Group H v3 - Knowledge References (utilities 174 through 179). Six new reference pages in calc-references.js: Hand Signal Reference (174 - crane/excavator/flagger/aircraft marshalling, no image reproduction), OSHA Top-10 Citations (175 - by 29 CFR section), Lockout/Tagout Steps (176 - 9-step procedure citing 29 CFR 1910.147 by section), Defensible Space Reference (177 - Zone 0/1/2 actions citing CALFIRE/NFPA by name only), Storm Shelter Spec Reference (178 - FEMA P-320 wind/anchorage/door/ventilation by name only), Field First Aid Triage Quick-Read (179 - START categories with hardened "not medical advice; call 911; see sophiewell.com" notice). Each renders an h2 plus dl pair following the existing reference-utility pattern. Registered in REFERENCE_RENDERERS, TOOL_MODULES, TOOL_DATA_SOURCES (all six map to a new `data/summaries/v3-references.json` shard added to scripts/build-data.mjs), and TOOLS in group H. 31 new unit tests in test/unit/calc-references-v3.test.js covering registry shape, citation invariants, START category coverage, and the triage notice content. The existing `REFERENCE_RENDERERS exposes 5 ids` test updated to 11. Total v3 utilities now wired: 145-179 + 182, 183, 185, 187 (the implemented Group I subset). Pending: utility 180 Job Pack (UI over recents + bundle), utility 181 QR of current URL (needs a bundled MIT QR library), utility 184 Numpad layout toggle, utility 186 Send To Other Device.

- Step 42 complete (spec-v3.md): Group F v3 - Fire-Ground Round Three (utilities 159 through 161). Added `computeConfinedSpacePurge` (t = V * N / CFM with OSHA 1910.146 cited by section), `computeRopeMA` (theoretical MA per rig type x efficiency^pulleys; haul force = load / actual MA; rigs 1:1 through 5:1 piggyback and T-method), and `computeSlingAngle` (L = W / (n * sin(theta/2)) for basket/bridle, vertical L = W/n, choker reduction factor 0.75 applied; ASME B30.9 cited by section) in calc-fire.js. All three carry the SOP-and-incident-command notice plus the worker-safety variant. Three renderers added. Tools registered in TOOLS, TOOL_MODULES. Two new derivation sections in docs/derivations.md (capstan-and-pulley MA with friction; sling angle leg tension). 30 new unit tests in test/unit/calc-fire-v3.test.js including the limit case where small included angles diverge tension.

- Step 41 complete (spec-v3.md): Group E v3 - Carpentry and Construction Round Three (utilities 147 through 158). Added `computeDrywall` (sheet count + mud + tape + screws), `computeRoofingSquares` (squares with pitch-based waste, bundles, underlayment, drip edge), `computeAsphaltTonnage` (volume * density / 2000; 20-ton truck loads), `computeAggregate` (cubic yards + tons by material density), `computeMortarMix` (PCA yields with joint-thickness adjustment for brick / 8-inch CMU), `computeConcreteMixDesign` (ACI 211 simplified: w/c interpolation by strength and exposure; water from aggregate + slump; cement, coarse, fine aggregate per yd^3), `computeBoltTorque` (T = K * D * F with grade proof loads and tensile area), `computeBendAllowance` (BA = (pi/180) * angle * (R + K * t); flat blank with setback), `computeSpeedsAndFeeds` (RPM = SFM * 3.82 / D; IPM = RPM * chipload * flutes), `computeWeldUsage` (deposit = section * length * 0.283; consumable = deposit / efficiency; SMAW/GMAW/FCAW/GTAW), `computeDemoDebris` (tons by structure pcf with 10/20/30/40 yd^3 dumpster), and `computeFormworkPressure` (ACI 347 short form P = C_w * (150 + 9000R/T) capped at wet head). Twelve renderers added (using a compact `_simpleRenderer` factory for v3 utilities). Tools registered in TOOLS, TOOL_MODULES, TOOL_DATA_SOURCES (concrete-mix-design, bolt-torque, speeds-feeds, weld-usage data-driven). Four new data shards added: aci-211-curves.json, bolt-grades.json, sfm-table.json, aws-deposition.json (all cited by name only). Six new derivation sections (mix design, bolt torque, bend allowance, speeds/feeds, AWS deposition, formwork pressure). 120 new unit tests in test/unit/calc-construction-v3.test.js with explicit boundary tests for the formwork wet-head cap and the 10/20/30/40 yd^3 dumpster steps.

- Step 40 complete (spec-v3.md): Group D v3 - Restoration Round Three (utilities 145 and 146). Added `computeContainmentAirBalance` (orifice flow Q = 2610 * A * sqrt(delta_P) with NAM count recommendations from utility 87 logic) and `computeChamberTurnover` (ACH = (air mover total cfm + dehu cfm) * 60 / chamber volume; gap to target). Two renderers added. Tools registered in TOOLS, TOOL_MODULES. One new derivation section in docs/derivations.md (containment orifice flow). 20 new unit tests in test/unit/calc-restoration-v3.test.js with explicit error cases. After Steps 40-42: 1212 unit tests passing. data:refresh wrote 50 shards across 9 datasets; data:verify clean (59 entries). Home-view payload 29.0% of 100 KB budget.

- Step 39 complete (spec-v3.md): Group C v3 - HVAC Round Three (utilities 139 through 144). Added `computeAffinityLaws` (target by RPM/CFM/SP/kW with cube/square/linear ratios), `computeBeltAndPulley` (V-belt L = 2C + (pi/2)(D+d) + (D-d)^2/(4C); driven RPM via diameter ratio; belt speed in fpm), `computeAirReceiver` (V = (t * (C_demand - C_pump) * P_atm) / (P1 - P2) with tools-at-duty-cycle list; ft^3 to gallons via 7.4805; concurrent-tools prefix-sum count), `computeGeothermalLoop` (max(heating, cooling) / BTU-per-ft from DOE-style benchmarks per soil and loop type; rock-horizontal explicitly unsupported), `computeBaseboardOutput` (manufacturer-attributed BTU/ft tables interpolated by water temp; flow correction; Slant/Fin and high-capacity models), and `computeNPSHa` (H_atm from elevation lapse, H_vapor from a public engineering psi-by-temp table converted via 2.31 ft/psi, static and friction heads; cavitation flag against optional NPSHr). Six renderers added. Tools registered in TOOLS, TOOL_MODULES, TOOL_DATA_SOURCES (geothermal-loop, baseboard-output data-driven). Three new data shards added to scripts/build-data.mjs: affinity-laws.json (example shard), baseboard-output.json (Slant/Fin attribution), geothermal-soil.json (DOE). Four new derivation sections in docs/derivations.md (affinity laws, V-belt length, air receiver, NPSHa). 60 new unit tests in test/unit/calc-hvac-v3.test.js including a regression test that NPSHa flags cavitation when source-elevation negative and friction high. 1042 unit tests passing. data:refresh wrote 46 shards across 9 datasets; data:verify clean (55 entries). Home-view payload 27.9% of 100 KB budget.

- Step 38 complete (spec-v3.md): Group B v3 - Plumbing and Gas Round Three (utilities 132 through 138). Added `computeStormwaterRational` (Q = C * i * A with bundled coefficients per surface; cfs and gpm), `computeManningSlope` (English Manning V = (1.486/n) * R^(2/3) * S^(1/2) solved for self-cleansing slope at 2 ft/s and the slope to carry target flow at half-full with R = D/4), `computeHydrostaticTest` (test pressure 1.5x water / 1.25x gas with optional override; piecewise hold-time recommendation), `computeGreaseTrap` (volume = peak_gpm * retention * loading_factor; PDI G101 cited; recommended nominal from a 14-step ladder), `computeGlycolMix` (linear interpolation across manufacturer freeze-point curves for propylene and ethylene; concentrate gallons), `computeExpansionTank` (V_tank = V_sys * ((rho_cold/rho_hot) - 1) / (1 - P_i/P_f) with absolute pressures and a public water-density table), and `computeBackflowLoss` (linear interpolation of manufacturer pressure-loss curves by device class and pipe size) in calc-plumbing.js. Seven renderers added. Tools registered in TOOLS, TOOL_MODULES, TOOL_DATA_SOURCES (stormwater-rational, manning-slope, glycol-mix, backflow-loss data-driven). Four new data shards added to scripts/build-data.mjs: runoff-coefficients.json, manning-roughness.json, glycol-curves.json (Dow attribution), backflow-curves.json (Watts attribution). Four new derivation sections in docs/derivations.md (rational method, Manning slope, expansion tank, hydrostatic test). 71 new unit tests in test/unit/calc-plumbing-v3.test.js with explicit error cases (zero/negative inputs, unknown selectors, out-of-range targets). 982 unit tests passing. data:refresh wrote 43 shards across 9 datasets; data:verify clean (52 entries). Home-view payload 27.5% of 100 KB budget.

- Step 37 complete (spec-v3.md): Group A v3 - Electrical Round Three (utilities 125 through 131). Added `computePullingTension` (capstan equation T_out = T_in * exp(mu * theta) accumulated per bend, sidewall pressure T/R per bend, head-end and sidewall flags against 5000 lb / 1000 lb-per-ft thresholds), `computeBendRadius` (manufacturer-attributed multiples of cable OD: THHN/XHHW 8x, MC 7x, control 6x, coax 10x, fiber 20x), `computePFCorrection` (kVAR = kW * (tan(acos(PF1)) - tan(acos(PF2))); microfarads from Q = V^2 * 2*pi*f*C at 60 Hz with three-phase Y per-leg form), `computePhaseBalance` (per-phase totals, imbalance percent over average, greedy heaviest-to-lightest swap suggestion), `computeMultiLoadVoltageDrop` (cumulative I*R per segment along ordered loads, single-phase round-trip), `computeLVDCDrop` (12/24/48 V DC drop with application-tolerance flag table: LED 3%, marine 10%, marine-critical 3%, RV 5%, audio 2%), and `computePoEBudget` (IEEE 802.3 af/at/bt3/bt4 budgets, Cat5e/6/6A loop resistance with copper-alpha temperature correction, green/amber/red flag against per-class PD minimum) in calc-electrical.js. Seven renderers added; ELECTRICAL_RENDERERS extended; the seven new tools registered in TOOLS, TOOL_MODULES, and TOOL_DATA_SOURCES (cable-bend-radius and poe-budget data-driven). Two new data shards added to scripts/build-data.mjs: data/electrical/cable-bend-radius.json (manufacturer-attributed: Southwire, AFC, Belden, Corning) and data/electrical/poe-classes.json (IEEE 802.3 cited by name only; Belden / CommScope category resistance benchmarks). Four new derivation sections in docs/derivations.md (capstan equation, PoE budget, PF correction kVAR, multi-load voltage drop). 72 new unit tests in test/unit/calc-electrical-v3.test.js across all seven utilities including explicit-error cases (zero current, negative gauge, empty bend list, unknown PoE class, etc.). 911 unit tests passing. data:refresh wrote 39 shards across 9 datasets; data:verify clean (48 entries). Home-view payload 27.0% of 100 KB budget (calc-electrical.js loads on first electrical-tool view via dynamic-import).

### Repo cleanup for public release

- Repo audited and cleaned for public release. Deleted: `encryptalotta/` (a 3.6 MB foreign git repo accidentally nested inside roughlogic-dev during the theme-port reference work), `logo.jpg` (178 KB brand source; the favicon.svg shipped at runtime is the live brand mark), `.eslintrc.json` (no npm script invokes ESLint; the lint contract is grep + n-gram + home-payload checks), `dist/` (build output; regenerated by `npm run build`), and `test-results/` / `playwright-report/` (Playwright failure artifacts; regenerated each run). `scripts/grep-checks.mjs` `TOKEN_CHECK_SKIP` no longer references the removed `.eslintrc.json`. `.gitignore` extended to explicitly cover `test-results/`, `playwright-report/`, and the `probe-tmp.mjs` interactive-development files. `scripts/derivations.md` index updated to list all 24 derivations (v1 1-11 + v2 12-24) instead of the v1-only list. Repo size dropped from 29 MB to 20 MB; the deployable surface (everything not gitignored) is even smaller.

- Real silently-broken behavior fixed: arrow-key tile navigation. The keyboard handler in `bindShortcuts()` did `getElementById("tile-grid")` and skipped wiring when null. The v2 render-by-group system replaced the single `<ul id="tile-grid">` with `<div id="tools-sections">` containing N `<ul class="tile-grid">` (one per group A through H), so `tile-grid` as an id no longer existed and arrow-key navigation was silently inert from the v2 expansion onward. Re-pointed the listener at `#tools-sections` (with a fallback to `#tile-grid` for any future reversion); the inner `querySelectorAll(".tile-link")` already walks every `<ul class="tile-grid">` block in document order so Tab + Arrow-Right traverses tiles across group boundaries seamlessly. Verified end-to-end in real Chromium: focusing Ohm's Law (Group A tile 1), pressing ArrowRight focuses Wire Ampacity (Group A tile 2); ArrowDown focuses Box Fill (Group A tile 5, row 2). New integration regression guard "Arrow-key navigation works across v2 group sections" added; 45 / 45 integration tests pass.

- One dead CSS class removed: `.tile.is-hidden { display: none; }`. With the v2 render-by-group system, filtered-out tiles are simply not rendered into the DOM (renderHome iterates groups and only appends tiles that pass `matchesFilters`); the `is-hidden` class was never added to a tile by JS. Confirmed by a node script that grep'd every CSS class against HTML + every JS file in the repo: only one class (`is-hidden`) had zero references outside CSS itself. Removed.

- README "Quick start" tightened: "Select a tool" replaced with "Click the tile to open the calculator", reflecting the new card-link behavior where the entire tile body is clickable (not just the small "Open tool" button).

- Card-link Cmd/Ctrl-click opens the tool in a new tab. Previously the tile click handler returned early on any modifier-key click (Cmd / Ctrl / Shift / middle-click), so power users could not multi-pop tools without using the explicit "Open tool" anchor's right-click menu. Now Cmd/Ctrl-click on the tile body calls `window.open(href, "_blank", "noopener")` to open the tool in a new tab; Shift / middle-click still skip (matching the explicit-link semantics where Shift opens in a new window and middle-click is browser-handled). Verified end-to-end in real Chromium: Cmd-click on the title area of "Ohm's Law" opens `#ohms-law` in a new tab while the original tab stays on home.

- Card-link UX pattern: the entire tile body is now clickable. Previously only the explicit "Open tool" link inside each tile was clickable - clicking the title, description, tags, or whitespace did nothing. encryptalotta makes the whole card a button; roughlogic now matches. styles.css adds `cursor: pointer` to `.tile` and `position: relative; z-index: 2` to `.tile-pin` so the Pin button stays separately interactive. app.js `buildTile` adds a `click` handler on the LI that calls `navigateTo(tool.id)` unless the click landed on `.tile-pin` (own handler), `.tile-link` (native href navigation), modifier-key clicks (Cmd/Ctrl/Shift = open in new tab), or middle-click. End-to-end verified in real Chromium: click in title area opens the tool, Pin click toggles state without navigating, "Open tool" link click navigates as before. New integration regression guard "entire tile body is clickable (card-link pattern), Pin stays separate" added; 44 / 44 integration tests pass.

- Structural audit verified all 119 tool ids in `TOOLS` are also registered in `TOOL_MODULES` (zero mismatches in either direction): every tile clicks through to a real renderer, every renderer has a tile. 115 `compute*` functions across the eight calc-*.js modules; the 4 tools without a compute step are pure-data reference utilities (backflow, smoke-reading, and similar) that render bundled data directly. All keyboard-shortcut targets, CI workflow npm-script references, and HTML asset references resolve to real entities. No invariants regressed.

- CI workflow updated to use the now-distinct `test:a11y` and `test:e2e` scripts. Previously the `accessibility` job ran the full 43-test integration suite via a hand-rolled `npx playwright test --config=...` invocation - the job's name suggested a11y-only coverage but it included smoke / layout / theme / viewer tests too. Renamed the existing job's command to `npm run test:a11y` (now 27 axe-core route scans, ~9 s; same `needs: test` gating), and added a parallel `integration` job that runs `npm run test:e2e` for the full 43-test suite. Both run in parallel after the lint+unit+data-integrity job. Comment headers added to ci.yml documenting what each job covers.

- `npm run test:e2e` and `npm run test:a11y` were byte-identical commands - both ran the same Playwright config which runs all 43 tests. Split per spec section 13's two named test passes: `test:e2e` continues to run the full integration suite (43 tests; axe-core routes + smoke + layout + theme + viewer), while `test:a11y` now runs only the 27 axe-core route scans via `--grep 'a11y:'`. CI workflows that want a fast pre-merge a11y signal without the smoke overhead can now invoke `npm run test:a11y` (about 9 seconds, was 16 seconds for the full suite). Both scripts honor the same playwright.config.js webServer setup and the same test file.

- Integration regression guard for the static changelog viewer. The /changelog.html page fetches CHANGELOG.md and parses it with a deterministic Markdown-lite parser (heading-only logic, no innerHTML, no library). New test "changelog.html renders headings and list items from CHANGELOG.md" loads the page, waits for the rendered structure, asserts `>2` h2/h3 headings (covers v0.2.0 + v0.1.0 release headings + the build-progress subheadings), `>20` list items, the theme toggle is present, and zero JS errors. If a future change breaks the fetch path, the parser, or the styles.css `#changelog-content` selectors, the test catches it before deploy. 43 / 43 integration tests pass.

- docs/launch-checklist.md preamble clarified to prevent v0.1.0 historic numbers from being mistaken for current state. The document is structured as the v0.1.0 launch report (frozen snapshot - 220 unit tests, 53 files / 290 KB dist, the now-removed Changelog/Data-version footer lines, the now-removed filter-button rows) followed by a "v0.2.0 deploy-time gates" diff section. Without a structural note, a fresh reader could read the v0.1.0 Operations row "Verified by `npm run build`; 53 files / 290 KB total dist" as a current-state claim. Added a blockquote at the top explaining the structure: when numbers disagree between the two sections, the v0.2.0 stanza is the current state.

- Progressive-enhancement regression guard added. New integration test "noscript notice renders and is visible when JavaScript is disabled" launches a browser context with `javaScriptEnabled: false`, navigates to /index.html, and asserts: the `.noscript-notice` element exists exactly once, is visible, contains the strings "JavaScript" and "locally", and zero tiles render. Static-site / progressive-enhancement contract is now fail-closed in CI - if a future change moves the notice into a JS-rendered region, the test catches it. Previously verified manually but not locked in. 42 / 42 integration tests pass.

- More dead-code cleanup in app.js: six exported symbols never imported anywhere. `pinTool` and `unpinTool` were `export function ...` definitions used by no other module; tile and tool-view click handlers do their own pin toggling inline. The trailing line `export { TOOLS, TRADES, GROUPS, NOTICE_DEFAULT, NOTICE_FIRE };` exposed five internal constants for "tests and future calculator modules" but app.js can't be cleanly imported by tests (DOMContentLoaded side effect on import) and no calculator module ever imported any of these. Removed both blocks; the constants stay as internal `const` declarations and `capitalize` (the only remaining helper) is verified still used by `buildTile`. app.js raw size dropped from 55912 to 55275 bytes; home-view gzipped payload from 26.1% to 26.0% of the 100 KB budget. 839 unit tests still pass; integration suite 41 / 41 still pass.

- Dead-code cleanup in app.js and routing.js. (1) `decodePinned` was defined in app.js as a thin wrapper around `decodePinnedList(s, new Set(TOOLS.map((t) => t.id)))` but was never called - removed. (2) The import line `import { parseHashRoute, decodePinnedList, decodeIdList, toolMatches } from "./routing.js"` brought in `decodePinnedList` and `decodeIdList` neither of which app.js uses anymore (parseHashRoute decodes the lists internally) - import line trimmed to `{ parseHashRoute, toolMatches }`. (3) routing.js had two byte-identical implementations of `decodePinnedList` and `decodeIdList` - collapsed `decodePinnedList` to a re-export alias of `decodeIdList`. The existing routing.test.js suite still imports both names and continues to pass. routing.js raw size dropped from 2729 to 2685 bytes; net home-view gzipped payload stays at 26.1% of the 100 KB budget. 41 / 41 integration tests still pass.

- Comprehensive code audit. Confirmed: every file referenced by `<script src=...>` and `<link href=...>` exists in the repo, every SW shell-cache asset and data manifest ships in dist/, zero `console.log/warn/info` and zero TODO/FIXME/XXX in shipped JS/CSS/HTML. Raw asset sizes (uncompressed): index.html 8 KB, styles.css 28 KB, app.js 56 KB, integrity.js 4 KB, routing.js 4 KB, theme.js 4 KB.

- Keyboard-Enter activation regression guard. The brand link and back-link no-scroll-to-top fix lives in a `click` event handler. Browsers fire `click` for both pointer-click and Enter on a focused anchor, so the fix is meant to apply to keyboard activation too. New integration test "Brand link and back-link route home via keyboard Enter without scroll-to-top" focuses each link, presses Enter, and asserts `window.scrollY === 0` and `#tools` is visible afterward. 41 / 41 integration tests pass.

- Brand link and "Back to tools" link no longer cause a scroll-to-top jump before routing home. Both used `href="#"` so the browser's default click handling scrolled to the document top *before* the hashchange handler routed home and rewrote the URL. Visible as a brief jump on every navigation back to home from a tool view. Now both links bind a click handler that calls `e.preventDefault()` and then `navigateTo("")` directly. The `href="#"` is kept so right-click "Open in new tab" still works (modifier keys / middle-click bypass the preventDefault). Verified end-to-end in real Chromium: clicking brand or back-link from a tool view leaves `scrollY=0`, swaps to home, and writes `#r=<recent-tool>` to the URL with no intermediate scroll-to-top frame.

- README.md "Documentation" section updated to match the actual docs/ tree. Two stale claims fixed: (1) "Public changelog at CHANGELOG.md and accessible from the footer" - the footer changelog link was removed earlier; updated to note the static page at /changelog.html instead. (2) The Documentation list was missing four entries that have been in the repo for a while: spec-v2.md, docs/performance.md, docs/deployment.md, docs/launch-checklist.md. Added all four with one-line descriptions matching their file headers. Also reformatted the existing entries to "name - description" style for consistency.

- Mobile header layout tightened. The mobile breakpoint had `.primary-nav { order: 2 }` left over from a previous header structure even though `.primary-nav` is no longer in the DOM (the Tools link was removed at the user's request). Replaced with explicit `order` declarations for the elements that DO exist: `.brand { order: 1 }` and `.theme-toggle { order: 2; margin-left: auto; }` for row 1, `.header-search { order: 3; flex: 1 1 100%; }` for row 2. Verified at 390 px viewport in real Chromium: brand and toggle vertically centered on row 1 (centers within 4 px of each other), search occupies row 2 below both. New integration regression guard "Mobile header lays out as brand+toggle row 1, search row 2" added to test/integration/a11y.test.js. 40 / 40 integration tests pass.

- Visual bug fixed on Group H reference views: an empty 50 px bordered `.input-region` box was rendering above the actual content. Reference utilities (color codes, knot reference, inspection checklist, emergency contacts, tool maintenance) put their content into the output region only and leave the input region empty - without a hide rule, the empty bordered panel reads as a layout glitch. Added `.input-region:empty { display: none; }` to styles.css. Verified end-to-end: color-codes input-region renders at 0 px height now (was 50 px), voltage-drop input-region still renders at ~613 px with all its actual fields. Two new integration regression guards added: "Group H reference views do not render an empty input-region panel" and "Calculator views still render a non-empty input-region". 39 / 39 integration tests pass.

- Three stale claims fixed in the integrity-check documentation. spec.md section 7, README.md "Safety guarantees", and docs/architecture.md "Integrity" section all said the integrity check verifies "the hash recorded in app.js" - but app.js never carried the manifest hashes; integrity.js fetches `data/integrity.json` (a build-time sidecar produced by scripts/build-data.mjs) at startup and compares against fetched manifest content. Updated all three to state the actual mechanism. docs/architecture.md also clarified that mismatch surfaces a non-blocking banner naming the affected dataset(s) - calculators still render so the user can decide whether to trust them. README.md mentions integrity.js by name and notes the non-blocking banner behavior.

- Comprehensive end-to-end smoke audit against dist/ in real headless Chromium. Every interaction listed below was exercised and confirmed working: (1) home renders 119 tiles in 8 group sections (Group A Electrical 18, B Plumbing 16, C HVAC 19, D Restoration 12, E Carpentry 21, F Fire 13, G Cross-Trade 15, H Knowledge 5); (2) header search live-filters to 1 tile when typing "ohm"; (3) typing in search from a tool view auto-routes back to home with the filter applied; (4) "no tools match" empty-state notice appears for unmatched queries and hides when cleared; (5) theme toggle flips data-theme between light and dark and persists across reloads via the `rl-theme` localStorage key; (6) every interactive element measures at least 48 px (tile-link, tile-pin, theme-toggle, view-share, copy-btn, back-link, view-print); (7) example=1 deep-link populates inputs and computes the answer (`#voltage-drop?example=1` -> 7.90 V drop); (8) tool view header has Pin/Copy share/Copy bundle URL/Download bundle/Print buttons all visible and functional; (9) pin toggle from a tool view flips "Pin to home" <-> "Unpin from home"; (10) Group F utilities show the SOP-and-incident-command notice variant ("Departmental SOPs and incident command govern all fireground operations.") while every other utility shows the standard notice; (11) keyboard shortcuts work (G+C navigates to Concrete Volume; ? opens the shortcut overlay); (12) integrity banner stays absent when data hashes verify clean. No JS errors anywhere. The "frame-ancestors ignored in <meta>" warning is gone after the recent cleanup. Static deploy via Cloudflare Pages from the dist/ directory remains the deployment model, with no server-side runtime, no cross-origin fetches, and no third-party domains.

- Real CSS specificity bug found and fixed: the HTML `hidden` attribute was being silently overridden by author CSS. When `clipboard.js`'s `addCopyAllButton` injected a Copy-all button on a Group H reference utility (e.g., `#color-codes`), it correctly set `btn.hidden = true` because reference views render `<dl>` lists with no labelled `<p>` rows for the button to act on. But `.copy-all-btn { display: inline-flex }` in author CSS outranked the user-agent `[hidden] { display: none }` rule by specificity, so the button stayed visible despite `hidden=true`. Verified end-to-end against dist/: Copy-all button rendered on every reference page with text "Copy all" but no labels under it. Fix: added a top-level `[hidden] { display: none !important }` so the `hidden` attribute always wins regardless of component-level `display` declarations. Now Copy-all is correctly hidden on `#color-codes` and correctly visible on `#voltage-drop` after its example button populates 2 labelled output rows. The same fix benefits any other element with a `display:` rule and an `el.hidden` toggle: the offline pill (currently not in DOM but the rule was vulnerable), the empty-state notice, the recents/pinned regions, and `<noscript>` blocks.

- Two new integration regression guards covering the above. `test/integration/a11y.test.js` adds: "Copy-all button stays hidden on Group H reference views" (loads #color-codes, asserts btn.count() === 1 and isVisible() === false), and "Copy-all button is visible on a calculator with labelled outputs" (loads #voltage-drop, clicks the example button, asserts the Copy-all button is now visible). 37 / 37 integration tests pass.

- Integration regression guard for the search-from-tool-view auto-route. New test: `search from a tool view auto-routes back to home with the filter applied`. Loads `#voltage-drop`, asserts the tool view is visible and the tools section is hidden, types `"ohm"` into the persistent header search bar, then asserts the view-region has hidden and the tools section is back with a filtered tile count between 1 and 19. Catches the silent-no-visible-change behavior the recent fix removed. 35 / 35 integration tests pass.

- Search bar now auto-routes to home when typed from a tool view. Previously, if a user was inside a calculator (e.g., #voltage-drop) and started typing in the persistent header search bar, `state.query` updated and `renderHome()` ran, but the home view was hidden so nothing visible changed. To see filtered results they had to manually navigate back to home. `bindSearch()` now detects when the user is on a tool view and the new query is non-empty, and calls `navigateTo("")` which swaps to the home view and shows the filtered grid. Empty queries do not auto-navigate (typing then clearing the field should not yank the user out of a calculator). Verified end-to-end: from #voltage-drop, typing "ohm" hides the view-region, shows the tools section, filters to 2 matching tiles, URL becomes "#r=voltage-drop" (recent tool preserved). 34/34 integration tests still pass.

- docs/accessibility.md and docs/launch-checklist.md reconciled with the current home-view layout. Three stale references fixed: (1) accessibility.md "v2 affordances" section claimed "the Group H filter button (`H`) on the home view follows the same `aria-pressed` toggling pattern" - the trade/group filter button rows were retired; replaced with copy describing the header search-bar-only filter and the eight `<section class="tools-section">` blocks each carrying an `<h2 class="tools-section-label">`; (2) accessibility.md print-stylesheet bullet listed elements that were renamed/removed; rewritten against the current rule set (header, copy/pin/share/bundle/print buttons, back link, footer badges hidden; inline notice, citation, inputs, output, source stamp preserved); (3) launch-checklist.md "Touch targets" row enumerated only filter buttons + inputs + example button; expanded to the current set (header search input, theme toggle, tile-link, tile-pin, view-pin / view-share / view-bundle-* / view-print, back-link, copy / copy-all) and noted Playwright bounding-box verification. The "v0.2.0 deploy-time gates" Accessibility line also updated from "aria-pressed on filter buttons including the new H group filter" to "semantic group sections rendered as `<section class='tools-section'>` with their own `<h2>` per group A through H."

- README.md user-facing copy reconciled with the current home view. Two stale claims fixed: "Quick start" said "Use the trade audience filter to narrow to your discipline" (the trade/group filter row was removed; the search bar is the only filter now); "How it works" said "tile grid with filters by trade audience and by group" (now eight group sections rendered top-to-bottom). Replaced with accurate copy that mentions the header search bar, the eight group sections (A through H), and the dark/light theme toggle that persists across reloads.

- Header theme-toggle integration test stabilized. The "header theme toggle touch target is at least 48x48 pixels" test was passing in isolation but intermittently failing in the parallel run. Cause: the header's `rl-fade-down` keyframe briefly translates the element 8 px above the viewport top, so `boundingBox()` returns the visible-clipped portion (height 40 px) until the keyframe settles. Added a 600 ms `waitForTimeout` so the test measures the resting position. Verified deterministic across three back-to-back full integration runs (34/34 each time).

- Spurious "frame-ancestors ignored" console warning silenced on every page load. Both index.html and changelog.html shipped a `<meta http-equiv="Content-Security-Policy">` that included `frame-ancestors 'none'` - but that directive is HTTP-header-only by CSP spec and is silently ignored when delivered via meta. Browsers logged "The Content Security Policy directive 'frame-ancestors' is ignored when delivered via a <meta> element." on every load (visible in earlier Playwright probes). Dropped `frame-ancestors 'none'` from both meta tags. The production `_headers` file still carries it (correctly delivered via HTTP), so clickjacking protection is unchanged. scripts/dev.mjs already serves the directive via the HTTP CSP header, so local dev parity is preserved. Verified end-to-end: page loads with zero console warnings.

- Three real bugs surfaced and fixed by running the full Playwright + axe-core integration suite end-to-end.

  1. **playwright.config.js webServer cwd bug.** The webServer command `npx -y http-server -p 8080 -c-1 .` was resolving `.` from `test/integration/`, not the repo root. Every page request 404'd; every a11y test was nominally "passing" only because `axe.analyze()` against an empty page produced an empty violations array. Added `cwd: "../.."` to anchor http-server at the repo root. Reproduced, confirmed the fix.

  2. **Animation-induced WCAG 2 AA contrast failures across all 27 axe routes.** The `rl-fade-down` (header) and `rl-fade-up` (view-region) keyframes ramped opacity from 0 to 1 over 0.5 / 0.4 seconds. axe-core sampled the page mid-animation and saw text composited at sub-full opacity (white-on-white at ~85% effective opacity, apparent foreground `#cbcbcb` on `#ffffff` -> 1.62:1) -> "color-contrast" violation, "serious" impact, on every route. Rewrote both keyframes to animate `transform` only; elements render at full contrast at every frame. The motion is preserved for users who want it; `prefers-reduced-motion` still disables it entirely.

  3. **Stale integration tests.** `test/integration/a11y.test.js` "filter buttons toggle aria-pressed" and "touch targets are at least 48x48 pixels" both targeted DOM elements that were removed (the trade/group filter row). Replaced with five tests that exercise the actual current DOM: theme toggle flips data-theme and persists across reloads (covers the rl-theme localStorage round-trip), home tile-link 48 px touch target, header theme-toggle 48x48 touch target, search-input narrows tile count when typed into, home renders all eight group sections.

  Final integration result: 34 of 34 Playwright + axe-core tests passing (was 27 phantom-passing on an empty page; now genuinely passing on the rendered v1 + v2 surface).

- Final pass to reconcile every "no localStorage" claim with the theme.js usage. Five remaining inaccuracies updated: README.md "Safety guarantees" line ("Read-only by default ... does not write to localStorage..."), spec.md section 7 ("No localStorage, sessionStorage, cookies, or IndexedDB. The service worker cache is the only client-side persistence."), spec.md section 11.5 ("URL hash carries pinned-tile state and calculator input state. No localStorage, no cookies."), docs/launch-checklist.md row "No localStorage / sessionStorage / cookies / IndexedDB | pass | None used.", and docs/launch-checklist.md v0.2.0 deploy-time-gates "Behavior" line. Each rewritten to state precisely what is and is not stored: localStorage holds one key (`rl-theme`, value `"light"` or `"dark"`); no sessionStorage, cookies, or IndexedDB; the URL hash remains the only state mechanism for tool data. docs/threat-model.md T11 wording also tightened. The project's contract now matches its code across spec.md, README.md, all four docs/ files, and CHANGELOG.

- app.js dead-code cleanup. After the trade/group filter row and the offline pill came out of index.html, `state.trade` / `state.group` were never written but were still threaded through `matchesFilters` -> `routing.toolMatches` on every keystroke, and the orphaned `bindFilters()` and `bindOfflinePill()` functions sat in source as un-called code. Dropped both fields from `state`, simplified `matchesFilters` to `toolMatches(tool, { query: state.query })`, and removed the two function definitions. `routing.toolMatches` itself keeps full trade/group support (defaulted to "all" when absent) so the routing.test.js suite continues to pass. Verified in real Chromium: 119 tiles render, search filters down to 1 tile when typing "ohm", no JS errors. Home-view payload edges down from 25.3% to 25.1% of the 100 KB budget.

- Boot path tightened. `boot()` no longer calls `bindFilters()` (the trade/group filter row was removed from index.html), `bindOfflinePill()` (the offline pill was removed from the footer), or the idle-deferred dynamic import of `data-stamp.js` for `stampFooterVersion()` (the data-version footer line was removed). All three were silent no-ops because the underlying DOM elements no longer exist; the third was actually fetching `data-stamp.js` on every cold home-view load just to call a function that returned immediately. Verified end-to-end against dist/ in real headless Chromium: data-stamp.js is no longer requested on the home view. The helpers themselves remain in source for reuse if any of the removed elements are restored. data-stamp.js continues to load on first tool view via `loadSupportLibs()` where it actually drives `stampDataSource()` for reference utilities. 119 tiles in 8 sections still render, 839 unit tests still pass.

- 48 px touch-target floor restored on every interactive element per spec.md section 11.6 / spec-v2.md section 1. After the encryptalotta theme port, several controls had drifted below the spec floor: `.tile-link` (40 px), `.tile-pin` (40 px), `.theme-toggle` (40 x 40 px square), `.copy-btn` / `.copy-all-btn` (no min-height; rendered around 28-30 px), `.filter-btn` for the "Clear recents" / "Clear pins" pill (36 px). Each promoted to `min-height: var(--touch-min)` (48 px) with horizontal padding bumped where needed to keep the visual pill shape. Verified end-to-end against the dist/ build in real headless Chromium: every measured element renders at exactly 48 px tall (tile-link, tile-pin, theme-toggle, view-share, copy-btn, back-link). 119 tiles still render in 8 sections; 839 unit tests still pass; home-view payload 25.2 KB / 100 KB.

- Light-mode WCAG AA fix for invalid-input and integrity-banner colors. The dark-mode validity reds (border `#ff5d6c`, reason text `#ff8c95`) read 2.99:1 and 2.23:1 on white - both fail WCAG AA's 4.5:1 minimum for body text. Promoted both colors to CSS variables (`--error-border`, `--error-text`) and added a light-mode override using `#c0392b` (5.4:1 on white) and `#a93226` (6.6:1 on white). Dark-mode behavior unchanged: same `#ff5d6c` border and `#ff8c95` reason text. The integrity-banner red border now also flows through the variable, so a tampered-data warning stays readable in either theme.

- Documentation reconciled with the theme.js localStorage usage. docs/threat-model.md T7 (Privacy leakage) updated from a blanket "no localStorage" claim to a precise statement that localStorage is used for one key (`rl-theme`, value `"light"` or `"dark"`) for theme persistence and nothing else; no calculator inputs, pinned set, recents ring, or bundle data is written there. docs/architecture.md "Persistence" section reconciled the same way. Both documents continue to assert the URL hash as the only state mechanism for tool data.

- scripts/check-home-payload.mjs now counts theme.js toward the 100 KB home-view first-paint budget. theme.js is loaded synchronously from `<head>` so it runs before the first paint to apply the saved theme without flicker; treating it as part of first-paint cost is honest. Payload total grows from 23.5 KB to 25.6 KB (25.0% of the 100 KB budget); ample headroom remains.

- theme.js (light/dark toggle) now ships to dist/ via scripts/build.mjs SHELL_FILES copy list. Previously theme.js existed at the repo root and was referenced by index.html / changelog.html `<script src="theme.js">` and by sw.js shell precache, but the build manifest didn't include it - so the deployed Cloudflare Pages copy was missing the file, which 404'd on every page load and silently left the theme toggle wired to nothing. theme.js also added to scripts/grep-checks.mjs TARGETS (passes the innerHTML / eval / em-dash / emoji audit). Verified end-to-end against the dist/ build served by `python3 -m http.server`: 119 tiles in 8 sections render, theme toggle flips `<html data-theme>` between `light` (body bg `#ffffff`) and `dark` (`#0a0a0a`) with zero JS errors. dist/ count grows from 79 to 80 files.

- styles.css rewritten to mirror encryptalotta.com's dark "delightful" theme. Same near-black layered surfaces (`#0a0a0a` page, `#1a1a1a` cards, `#2a2a2a` inputs), white-on-dark text with two muted secondary tints (`#c8c8c8` 8.5:1 and `#9a9a9a` 5.5:1 on the card background to keep WCAG AA), Apple-system font stack, sticky topbar with backdrop blur, rounded tile cards (240 px min, 14 px radius) with a small `->` arrow that fades in on hover, white pill buttons that go dark on hover, pill-shaped Tag chips and footer badges, gentle fade-up entry animations, and a `prefers-reduced-motion` opt-out. Every existing class name preserved (`.tile`, `.tile-grid`, `.filter-btn`, `.view-region`, `.view-pin`, `.view-share`, `.view-bundle-*`, `.view-print`, `.view-bundle-load`, `.pinned-header`, `.offline-pill`, `.copy-btn`, `.copy-all-btn`, `.integrity-banner`, `.inline-notice`, `.citation`, `.data-source-stamp`, `.tile-link`, `.tile-pin`, `.skip-link`, `.visually-hidden`, `#changelog-content` headings) so app.js and the calculator modules render unchanged. Touch targets remain 48 px; focus rings are a clear blue (`#4d90fe`) outline; the print stylesheet now flips back to white-on-black for ink. index.html / changelog.html / site.webmanifest updated to `color-scheme: dark` and `theme_color #0a0a0a` so the browser chrome and PWA install card match. Home-view payload after gzip: 23 KB / 100 KB (22.7% of budget). 839 unit tests still passing; lint and data:verify clean.

- 3 new unit tests in test/unit/build-sw-patch.test.js asserting the spec.md section 6 cache-name invariant: source sw.js retains the `dev-*` placeholder so local-dev caching works without a build, dist/sw.js (when present) has a 15-char `YYYYMMDDTHHMMSS` BUILD_HASH (no longer the placeholder), and dist/sw.js cache names include the BUILD_HASH constant. Tests skip cleanly in environments where dist/ has not been built yet (e.g. fresh CI checkout before the build step). 839 unit tests passing.

- Service worker cache name now keyed by build hash per spec.md section 6 ("Cache name includes the build hash; old caches are deleted on activation"). Previously sw.js shipped with a literal `dev-0001` placeholder so every deploy reused the same cache name; old caches were only deleted when the file list changed *and* the SW re-installed, which is a fragile invariant. scripts/build.mjs now reads the build timestamp and patches `const BUILD_HASH = "YYYYMMDDTHHMMSS";` into the shipped dist/sw.js (ISO 8601 basic format, second precision, 15 chars; e.g. `20260505T204419`). Each release thus produces unique `roughlogic-shell-<hash>` and `roughlogic-data-<hash>` cache names; the existing activate-time keys-difference cleanup deletes prior caches automatically. The build fails fast if the BUILD_HASH constant cannot be located in the source sw.js (a pattern-mismatch guard against future refactors). The source sw.js retains the `dev-0001` placeholder so local-dev caching still works without a build step.

- Tool count corrected from 118 to 119 across user-facing copy. Spec-v2.md authored utilities 65 through 118 (54 new); a follow-on landed utility 119 (Compare Two Refrigerants) implemented under Group C per spec-v2 Step 31. README "one hundred eighteen" -> "one hundred nineteen" everywhere it appeared. docs/launch-checklist.md "64 -> 118" gate corrected to "64 -> 119" with a footnote explaining utility 119 was added on top of the spec-v2 §2 numbering. CHANGELOG v0.2.0 release stanza updated likewise. The actual tile-grid count is 119 (`grep -c '{ id:' app.js`); the Lighthouse / a11y / unit suites continue to pass.

- docs/launch-checklist.md unit-test count updated from 802 (v0.2.0 cut) to 836 (current Unreleased state) and the axe-core view count updated from "16 representative views" to "27 representative views (16 v1 + 11 v2 routes spanning groups A through H)" so the post-deploy review reflects the actual coverage.

- scripts/analyze-data-changes.mjs taught the v2 shard shapes. The data-refresh PR-body generator drilled into v1 layers (values, rates, refrigerants, summaries) but treated v2 shards (charge_per_foot, equivalent-lengths, insulation, hepa-loading, soil-bearing, wind-snow-zones, irs-mileage, gsa-perdiem, demand-factors, lighting-density, material-expansion, septic-rules, v2-references) as opaque top-level objects, so a value change inside one of them surfaced as a generic shard-changed entry instead of a per-key added/removed/modified report. New DRILL_LAYER_KEYS list adds the v2 dictionary roots (oz_per_ft, feet_by_fitting_and_diameter, k_btu_in_per_hr_ft2_F, loading_per_CFM_hour_grams, allowable_psf, basic_wind_speeds_mph, ground_snow_loads_psf, rates_by_state, gfci_afci_by_area, benchmark_W_per_ft2, alpha_per_F, fixtures, gases, zones, species_grades, mixes, thresholds, materials, table) plus the original v1 layers, so the monthly data-refresh PR now drills into v2 shards too.

- Static changelog viewer (changelog.html) now styled. styles.css gained a scoped `#changelog-content` block that gives h2/h3/h4 headings consistent margins and font sizes, ul/li adequate spacing, and p paragraphs a margin-bottom. The viewer renders the whole CHANGELOG.md (Unreleased + v0.2.0 + v0.1.0) cleanly without spilling browser-default styling.

- scripts/grep-checks.mjs scope expanded to every project-authored JS module and doc per spec.md section 13. The TARGETS list previously covered index.html / styles.css / app.js / sw.js plus the user-facing docs, leaving every calc-*.js module, the new bundle.js / calc-references.js, and the support libs (pure-math.js, routing.js, hash-state.js, data-stamp.js, clipboard.js, ui-fields.js, ui-validity.js, integrity.js, changelog.js, manual-j-worker.js) outside the innerHTML / outerHTML / insertAdjacentHTML / eval / new Function / emoji / em-dash audit. Added all of them, plus the build/dev/data scripts and the v2-added docs (performance.md, deployment.md, launch-checklist.md). spec-v2.md is excluded because it is user-supplied spec text outside the project author's style rules. docs/legal.md added to TOKEN_CHECK_SKIP because it names forbidden APIs descriptively. Existing files all pass; no new code-level violations surfaced.

- Lighthouse CI URL list extended to cover v2 surface. lighthouserc.json now audits the home view, three v1 utilities (ohms-law, refrigerant-pt, friction-loss), and two v2 views: a Group H reference page (color-codes) and a Group A v2 calculator (service-load). The Group H view in particular renders a long `<dl>` list of color codes, so its inclusion catches any DOM-size or rendering regression on the v2 reference-utility shape. Same Lighthouse thresholds (>= 95 across performance / accessibility / best practices / SEO; FCP <1s, LCP <1.5s, TBT <100ms, CLS <0.05) apply.

- 4 new routing unit tests covering the v2 hash format. parseHashRoute correctly extracts `example=1` from a tool view URL alone, alongside other calculator-state params, and preserves arbitrary string values for the example flag (the app.js layer does the strict "1" comparison). Also documents that `#b=...` takes precedence over a `#p=...` form so a bundle URL is never silently demoted. 836 unit tests passing.

- docs/deployment.md updated for v0.2.0. The build-step list now names the v2 modules (calc-references.js, bundle.js) under the static-shell copy. A new "v0.2.0 release notes" section explicitly states no infrastructure changes are required: CSP unchanged, service worker shell precache extended for the new modules, home-view payload budget enforced by lint, Lighthouse thresholds unchanged. Pending items remain the same first-deploy gates listed in docs/launch-checklist.md.

- CHANGELOG.md ordering corrected to Keep-a-Changelog convention. The Unreleased section now appears before the v0.2.0 release stanza, which appears before v0.1.0. The static viewer at changelog.html / changelog.js renders headings deterministically and reads correctly under the new ordering.

- v2 tool-view header buttons styled to meet the 48 px touch target invariant (spec.md section 11.6 / spec-v2 section 1). The Step 32 / 33 / 34 / 35 buttons (`.view-share`, `.view-bundle-copy`, `.view-bundle-download`, `.view-bundle-load`, `.view-print`) had been styled only by the print stylesheet; they now share the same `min-height: var(--touch-min)`, padding, and border treatment as the existing `.view-pin` button, so the tool view header is gloved-hand operable on mobile. The Load Bundle file input is wrapped in an inline-flex labeled container with the same touch-target minimum.

- docs/architecture.md ASCII diagram extended for v2: app.js block now lists Recents, bundle.js, and the offline/print (Group I) affordances; a footer line names every dynamic-imported calc-*.js module (electrical, plumbing, hvac, restoration, construction, fire, cross, references). The home-view boxes (the file box layer) and the data folder list above it remain unchanged.

- Home-view payload budget per spec.md section 11.1 / spec-v2.md section 6 now enforced by an automated check. New scripts/check-home-payload.mjs gzips index.html, styles.css, app.js, integrity.js, and routing.js (the files loaded on first paint; calculator modules and support libs are dynamic-imported on first tool open) and fails the build if the total exceeds 102400 bytes. Wired into `npm run lint` and exposed as `npm run check:home-payload`. Current size: 21 KB (20.7% of budget). docs/performance.md updated with the new enforcement section.

- docs/derivations.md v2 sections renumbered into ascending order. Sections 12 through 24 had been inserted out of order during incremental v2 work (12 PV string, 17 deflection, 18 excavation, 19 wind, 20 snow, 21 anchor, 22 tandem-pump, 23 braking, 24 haversine appeared before 13 battery, 14 pipe expansion, 15 OA mixing, 16 insulation thickness). Reordered to read 12 -> 24 sequentially. No content changes; verification text and citations unchanged.

- Test-coverage parity per spec-v2.md section 7. Utility 70 (GFCI/AFCI Reference) now has 10 unit tests (was 6); utility 89 (Thermal Imager Delta-T Reference) now has 10 (was 6). Both data-driven reference utilities now meet the Step 23 / Step 26 "10+ tests per utility" promise. 832 unit tests passing.

- v2 first-principles verification suite per spec-v2.md section 6. test/unit/first-principles.test.js now covers PV string sizing (cold-Voc and warm-Vmp temperature corrections; max_series/min_series hand-calc values), battery runtime (Ah * V * DoD / load), pipe thermal expansion (alpha * L * 12 * dT for copper), joist mid-span deflection (5*w*L^4 / (384*E*I)), wind velocity pressure (q = 0.00256 * V^2), snow load (Pf = 0.7 * Ce * Ct * Is * Pg), anchor bolt embedment (T / (0.7 * sqrt(fc) * pi * d)), vehicle braking (v^2 / (30 * mu)), and haversine (1-degree-latitude ~ 69 mi; antipodal ~ pi * R). 10 new tests; 824 unit tests passing.

- v2 axe-core integration coverage per spec-v2.md section 6. test/integration/a11y.test.js gains 11 new representative routes covering one v2 utility per group (service-load, septic-tank, compare-refrigerants, standing-water, joist-deflection, braking-distance, loan-payment, haversine) plus three Group H reference views (color-codes, knot-reference, inspection-checklist) so the WCAG 2.2 AA assertion runs against every v2 surface.

- Project Bundle (utility 121) now encodes inputs for *every* calculator visited this session, not only the active one. New `mergeBundleInputs` helper in app.js merges the live tool view's input map onto carried bundle inputs (loaded from a #b=... hash, or accumulated across earlier tool views) before encoding. Restores the spec-v2 section 1 promise that a bundle is "the current pinned set + multiple calculator inputs". `collectBundleInputs` now skips the file-input used by Load bundle so it never appears in the encoded payload. 2 new bundle unit tests cover the multi-calculator round-trip and sanitize-against-live-ids behavior; 814 tests passing.

- Service worker shell precache extended to include the new v2 modules (calc-references.js for Group H Knowledge References and bundle.js for utility 121 Project Bundle). Without this, an offline navigation to a Group H tool or use of Project Bundle Copy / Download would miss the cache and fall through to network. The cache name remains keyed by build hash so the new shell list takes effect on the next deploy.

- v2 documentation pass per spec-v2.md section 6: docs/threat-model.md gains T10 (hostile bundle import) and T11 (URL-hash payload tampering) sections covering the v0.2.0 attack surface; docs/legal.md adds a v2-additions subsection inventorying the 13 new shards and their public / public-domain / manufacturer-attributed / original-work classifications; docs/accessibility.md adds a v2-affordances subsection covering the Group H filter button, Recents region, tool-view header buttons (Copy share link, Bundle actions, Print), the offline pill, the same-origin Blob download, and the print stylesheet; docs/architecture.md notes Group H in the home filter list, the Recents region, the new modules calc-references.js and bundle.js, and the v2 hash format (`p=...&r=...`, `b=...`, `example=1`); docs/performance.md updates the home-view payload statement (gzips to ~20 KB) and the lazy-load list to include the v2 modules. Offline pill given `aria-live="polite"` on its containing line so dictation users hear connectivity-state changes.

- Utility 119 complete (Compare Two Refrigerants). Side-by-side P-T view at a chosen pressure or temperature for any two refrigerants from data/hvac/refrigerants.json, surfacing each side's manufacturer attribution. Renderer wired into HVAC_RENDERERS and the tool registered in app.js TOOLS / TOOL_MODULES / TOOL_DATA_SOURCES under Group C. 10 new unit tests (mode round-trip, error paths, attribution surfaced, same-refrigerant equality); 812 unit tests passing. Lint and data:verify green.

- Step 36 complete (spec-v2.md): Documentation, integrity, and final review. README updated to "one hundred eighteen" tool count and the eight-group list, with a Cross-cutting platform line. package.json bumped to 0.2.0. CHANGELOG v0.2.0 release stanza summarizing the v2 expansion. docs/launch-checklist.md gains a v0.2.0 deploy-time gates section. data:refresh regenerated 37 shards across 9 datasets; 46 manifest hashes verified. npm test 802 passing; npm run lint and data:verify green. Home-view payload remains under the 100 KB budget (calc-*.js modules dynamic-imported).

- Step 34 complete (spec-v2.md): Group I - Offline Status Indicator (utility 123). New `<span id="offline-pill" hidden>Offline</span>` in the footer in index.html with a small inverted-pill style in styles.css. app.js binds `online`/`offline` events on window and toggles the pill from navigator.onLine. Service worker continues to serve the shell when offline; the pill is purely informational.

- Step 33 complete (spec-v2.md): Group I - Print / PDF View (utility 122). Extended @media print in styles.css to hide site-header, search/filter regions, primary nav, skip link, tile-pin, view-pin, copy buttons, tile-actions, integrity banner, recents/pinned regions, offline pill, the back link, and bundle/share/print buttons; preserves the inline notice, citation, inputs (with values), output region, and source stamp. New "Print this calculator" button in the tool view header that calls window.print().

- Step 32 complete (spec-v2.md): Group I - Project Bundle (utility 121). New bundle.js exports encodeBundle / encodeBundleHash / decodeBundle / sanitizeBundle. Bundle shape is `{ version: 1, pinned: [...ids], recents: [...ids], inputs: { [toolId]: { ...key/value } } }`. URL hash form is `#b=<base64url-JSON>` (32 KB cap, malformed-input rejection). routing.js parseHashRoute exposes `bundle` for the `b=` form; app.js parseHash decodes asynchronously, sanitizes against the live tool registry, and replaces the bundle hash with the resolved p=...&r=... form. Tool view header gains three new actions: "Copy bundle URL" (encodes current state + the active calculator's input map and copies to clipboard), "Download bundle" (creates a same-origin Blob URL and triggers an anchor download), and "Load bundle" (file input that decodes and replays via state and navigateTo("")). Bundle inputs auto-merge into the renderer's params on tool open. CSP unchanged; Blob URLs are same-origin. New test/unit/bundle.test.js with 18 cases (round-trip, malformed JSON, malformed base64url, version mismatch, size cap, sanitize against live ids, etc.); 802 unit tests passing.

- Step 35 complete (spec-v2.md): Group I - Example Deep-Link and Share Link (utility 124). renderToolView now adds a "Copy share link" button to the tool view header (lazily imports clipboard.js to avoid expanding the home payload). parseHashRoute params already round-trip through the URL hash; renderToolView now also recognizes `example=1` and clicks the renderer's "Test with example" button after the renderer mounts. The tool view URL stays bookmarkable and shareable.

- Step 31 complete (spec-v2.md): Group I - Recent Tools (utility 120). routing.js exposes `decodeIdList` and parseHashRoute now accepts a combined `#p=...&r=...` home form. app.js maintains state.recents (cap 10, no duplicates) via a `pushRecent` helper invoked whenever applyRoute lands on a tool view. New "Recents" home section above "Pinned" in index.html with id `recents-region`/`recents-grid` plus a "Clear recents" button. Hash encoder writes `r=...` alongside `p=...` so a recents bookmark restores the ring. 11 new routing-level unit tests plus 27 reference-utility tests; 784 total.

- Step 30 complete (spec-v2.md): Group H - Knowledge References (utilities 114 through 118) in the new calc-references.js. Five reference pages with original plain-English summaries: Wire / Pipe / Gas Color Codes (NEC, IEC, gas piping, ASME A13.1), Knot Reference (bowline, clove hitch, figure-eight, double fisherman, munter hitch with strength reductions), Inspection Prep Checklist (Electrical / Plumbing / HVAC / Carpentry rough-in and final), Utility Locator and Emergency Contacts (811, poison control, OSHA, NRC, 911), and Tool Maintenance Intervals. Each renderer outputs an h2 + dl pair with citation. New Group H added to GROUPS in app.js with a corresponding filter button in index.html. TOOL_DATA_SOURCES wired for all five (sourced from data/summaries/v2-references.json). New test/unit/calc-references.test.js with 27 cases; 774 unit tests passing at this step. Lint and data:verify green.

- Step 29 complete (spec-v2.md): Group G v2 - Cross-Trade Expansion (utilities 105 through 113) in calc-cross.js. Loan Payment uses payment = P*r / (1 - (1+r)^-n) with first 12 amortization rows. Upgrade ROI / Payback returns simple payback and 10-year NPV at user discount rate. Mileage and Fuel Cost computes gallons, fuel cost, and IRS reimbursement (rate from data/crosswalks/irs-mileage.json). Overtime applies 40 hr regular, 1.5x to a configurable double-time threshold, and 2.0x beyond. Per-Diem looks up GSA-published lodging or M&IE by state from data/crosswalks/gsa-perdiem.json. Geometry Pack covers circle (circumference, area, sector), ellipse (Ramanujan perimeter), regular hexagon, polygon perimeter, and sphere. Dilution / Mixing Ratio splits final volume into concentrate and diluent. Slope from Digital Level converts between degrees, percent, and inches per foot. GPS Distance uses the haversine formula plus the standard initial-bearing formula (R = 3958.8 mi / 6371.0088 km). New shards data/crosswalks/irs-mileage.json and data/crosswalks/gsa-perdiem.json wired into scripts/build-data.mjs (46 expected hashes). docs/data-sources.md, scripts/sources.md, and docs/derivations.md (section 24, haversine + bearing) updated. TOOL_DATA_SOURCES wired for mileage-cost and per-diem. New test/unit/calc-cross-v2.test.js with 90 cases; 747 unit tests passing. Lint and data:verify green.

- Step 28 complete (spec-v2.md): Group F v2 - Fire-Ground Engineering Expansion (utilities 100 through 104) in calc-fire.js. Reverse-Lay Supply Friction Loss extends the NFA CQ^2L formula to a tandem-pump scenario where the parallel section's per-pump friction scales as (1/n_pumps)^2. Sprinkler GPM Density returns total_gpm = area * density, with NFPA 13 hazard categories referenced by name only and a meets-minimum check. Standpipe Friction Loss sums riser elevation (0.434 psi/ft) plus per-outlet CQ^2L. Ladder Pipe Reach combines the existing aerial-ladder geometry with master-stream forward reach (cos 30 deg). Vehicle Braking Distance uses d = v^2 / (30 * (mu +/- grade%/100)) plus reaction distance v*1.467*t. docs/derivations.md (sections 22 tandem-pump, 23 braking) updated. New test/unit/calc-fire-v2.test.js with 50 cases; 657 unit tests passing at this step (cumulative 747 after Step 29). Lint and data:verify green.

- Step 27 complete (spec-v2.md): Group E v2 - Carpentry and Construction Expansion (utilities 90 through 99) in calc-construction.js. Stair Stringer Length uses Pythagorean over rise + run plus 2x12 board-foot estimate. Joist Mid-Span Deflection uses delta = 5 * w * L^4 / (384 * E * I) with L/360 and L/240 checks. Footing Area for Soil Bearing divides column load by allowable psf from the new soil-bearing.json shard, rounding side up to the next 6 in. Tile Count and Grout Volume sums waste-rounded tile count and grout cubic inches via joint perimeter * width * tile thickness. Paint Coverage uses 350/250/175 ft^2/gal benchmarks with porosity surface factors. Excavation Volume uses the frustum formula V = D/3 * (A1 + A2 + sqrt(A1*A2)) with set-back per side from depth and slope angle. Brick / CMU Count uses face-area waste-rounded count with mortar joint inflation. Wind Velocity Pressure applies the public ASCE 7 form q = 0.00256 * V^2 with windward (+0.8) and leeward (-0.5) Cp. Flat-Roof Snow Load applies Pf = 0.7 * Ce * Ct * Is * Pg. Anchor Bolt Embedment solves ld = T / (0.7 * sqrt(fc) * pi * d). New shards data/construction/soil-bearing.json and data/construction/wind-snow-zones.json wired into scripts/build-data.mjs (44 expected hashes). docs/data-sources.md, scripts/sources.md, and docs/derivations.md (sections 17 deflection, 18 excavation frustum, 19 wind, 20 snow, 21 anchor embedment) updated. TOOL_DATA_SOURCES wired for footing-area, wind-pressure, and snow-load. New test/unit/calc-construction-v2.test.js with 100 cases; 607 unit tests passing. Lint and data:verify green.

- Step 26 complete (spec-v2.md): Group D v2 - Restoration Expansion (utilities 86 through 89) in calc-restoration.js. Standing Water Volume uses gallons = area_ft^2 * depth_in / 12 * 7.48052 with weight at 62.4 lb/ft^3. Negative Air Machine Sizing uses required_CFM = volume * ACH / 60 and recommends unit counts at 500 / 1000 / 2000 CFM. HEPA Scrubber Filter Life Estimator uses filter_days = capacity_grams / (CFM * hours_per_day * loading_rate) with low/medium/high category rates. Thermal Imager Delta-T Reference is an original plain-English summary of typical surface-temperature differentials (NFA / OSHA training generally). New shard data/restoration/hepa-loading.json wired into scripts/build-data.mjs (42 expected hashes). docs/data-sources.md and scripts/sources.md updated. TOOL_DATA_SOURCES wired for thermal-delta-t. New test/unit/calc-restoration-v2.test.js with 37 cases; 507 unit tests passing. Lint and data:verify green.

- Step 25 complete (spec-v2.md): Group C v2 - HVAC Expansion (utilities 79 through 85) in calc-hvac.js. Refrigerant Charge Weighing sums oz per ft per line diameter per refrigerant from the new charge-per-foot.json shard. Approach and Delta-T Diagnostics computes condenser approach (T_sat - T_outdoor) and supply/return delta-T with low/normal/high banding. Outdoor Air Mix mass-mixes humidity ratios via the existing psychrometric helpers and weighted dry-bulb. Equivalent Length of Fittings sums the new equivalent-lengths.json table by fitting type and diameter. Wet-Bulb Sling Psychrometer solves the standard psychrometric wet-bulb relation for RH, dew point, and GPP. Pipe Insulation Thickness bisects r2 on Q = 2*pi*k*L*dT / ln(r2/r1) against an outside-film flux. Latent Heat Evaporative Cooling uses Q = m_dot * h_fg with h_fg ~ 1054 BTU/lb. New shards data/hvac/charge-per-foot.json, data/hvac/equivalent-lengths.json, and data/hvac/insulation.json wired into scripts/build-data.mjs (41 expected hashes). docs/data-sources.md, scripts/sources.md, and docs/derivations.md (sections 15 OA mixing and 16 cylindrical conduction) updated. TOOL_DATA_SOURCES wired for refrigerant-charge, equivalent-length, and insulation-thickness. New test/unit/calc-hvac-v2.test.js with 70 cases; 470 unit tests passing. Lint and data:verify green.

- Step 24 complete (spec-v2.md): Group B v2 - Plumbing and Gas Expansion (utilities 72 through 78) in calc-plumbing.js. Water Hammer Arrestor designation by PDI WH-201 method (six designations AA-A through AA-F). Hot Water Recirculation Pump Head reuses Hazen-Williams over the supply loop plus an equivalent-length allowance for fittings. Septic Tank Sizing applies 150 gpd per bedroom (or explicit gpd) with a 1000 gallon floor and the 2x daily-flow rule. Trap Arm Length combines a per-diameter table with a fall-limit (one pipe diameter total fall). Pipe Thermal Expansion uses dL (in) = alpha (1/F) * L (ft) * 12 * dT (F) with NIST / manufacturer-bulletin coefficients. Tankless Water Heater GPM uses gpm = kBTU * 1000 / (8.33 * 60 * dT) with NOAA inlet temperatures by climate zone. Gas Leak Rate uses Q = 3550 * c * A * sqrt(dP / SG). New shards data/plumbing/material-expansion.json and data/plumbing/septic-rules.json wired into scripts/build-data.mjs (38 expected hashes). docs/data-sources.md, scripts/sources.md, and docs/derivations.md (section 14, pipe thermal expansion) updated. TOOL_DATA_SOURCES wired for water-hammer-arrestor, trap-arm, and gas-leak-rate. New test/unit/calc-plumbing-v2.test.js with 70 cases; 400 unit tests passing. Lint and data:verify green.

- Step 23 complete (spec-v2.md): Group A v2 - Electrical Expansion (utilities 65 through 71) in calc-electrical.js. Service Load Calculation (Residential) sums general lighting (3 W/ft^2), small-appliance (2 x 1500 W default), laundry (1500 W), fixed appliances, range (first 8 kW at 100% then 40%), dryer (5 kW minimum), and HVAC (larger of cool vs heat); applies the standard residential demand factor (first 3000 W at 100% then 35%) and outputs required ampacity at 240 V plus the next standard service from [60, 100, 125, 150, 175, 200, 225, 250, 300, 400]. Generator Sizing returns continuous and surge wattage (surge = continuous + the largest motor's starting-running excess). Solar PV String Sizing computes cold-Voc max-series and warm-Vmp min-series for the inverter's MPPT and Vdc-max window with an infeasibility flag. Battery Runtime computes hours from Ah * V * DoD / load with Peukert form t = C / I^k when k > 1. Voltage Imbalance returns percent imbalance and the NEMA-form motor derate factor (1 - 2 * (imbalance/100)^2). GFCI/AFCI Requirements Reference and Lighting Power Density use plain-English benchmarks; NEC and ASHRAE 90.1 are referenced by section/name only. New data shards data/electrical/demand-factors.json, data/electrical/lighting-density.json, and data/summaries/v2-references.json wired into scripts/build-data.mjs and integrity (36 expected hashes; 9 manifests). docs/data-sources.md and scripts/sources.md updated. TOOL_DATA_SOURCES wired for gfci-afci-reference and lighting-density. New test/unit/calc-electrical-v2.test.js with 68 cases across the seven utilities; 330 unit tests passing. Lint and data:verify green.

### Polish

- Static HTML changelog view at changelog.html with same-origin styling, fetched and rendered as plain text by changelog.js (no innerHTML, no Markdown library); replaces the raw CHANGELOG.md link in the footer per spec section 14 step 20.
- scripts/derivations.md added per spec section 6 file layout; the authoritative record remains in docs/derivations.md.
- Brand mark added: favicon.svg is a monochrome 18-tooth gear that matches the user-supplied logo.jpg (brand source kept in the repo root, not shipped to dist). icon and apple-touch-icon link tags reference the same SVG so the runtime payload stays minimal. site.webmanifest icons array updated.
- Hash state wired per spec section 11.5. New hash-state.js exposes applyHashState (populate inputs/selects/checkboxes from URL params on tool open) and wireHashState (write current input state back to the URL hash via history.replaceState, debounced 100 ms). app.js renderToolView calls both for every calculator so calculator state is bookmarkable and shareable without localStorage. Pin / Unpin buttons added to every tile (toggling state.pinned and re-rendering with aria-pressed) and a "Pin to home / Unpin from home" button added to each tool view header. styles.css gains tile-actions, tile-pin, view-header-row, and view-pin styles preserving 48 px touch targets and the monochrome palette. Five new hash-state unit tests, 225 total passing.
- Pinned section rendered at the top of the home view (id="pinned-region") with a "Clear pins" affordance. Visible only when state.pinned is non-empty. Pin/unpin from a home tile or via the home-route applyRoute now writes the hash to #p=... per spec section 11.5; navigating to a #p=... bookmark restores the pinned set. Pinning from a tool view updates in-memory state; the URL is rewritten with #p=... the next time the home route is rendered. styles.css adds .pinned-header and .pinned-title.
- Per spec section 11.7, every reference-style utility now displays a "Source: <dataset>, version <X>, fetched <Y>." line on its view. New data-stamp.js exposes fetchManifest (cached), stampDataSource (appends a per-view source line from the relevant data/<folder>/manifest.json), and stampFooterVersion (replaces the "Data version: pending" footer text with the build timestamp from build-info.json or the physical-constants manifest version). app.js carries a TOOL_DATA_SOURCES table mapping 21 reference-style tool ids to their manifest folder/shard/label; renderToolView calls stampDataSource for matching ids. First-principles calculators continue to cite physics inline only. New .sources-region and .data-source-stamp styles in styles.css.
- Runtime data-integrity check per spec section 7. scripts/build-data.mjs now emits data/integrity.json containing the SHA-256 hash of each per-folder manifest. New integrity.js fetches that sidecar at boot and verifies every manifest via SubtleCrypto SHA-256. A mismatch (or missing/unfetchable manifest) emits a console error and inserts a non-blocking banner above the main content naming the affected dataset(s). Skips gracefully when SubtleCrypto is unavailable (e.g., http://example, dev fallback). 4 new unit tests using a fetch + DOM stub and Node's webcrypto, 229 total passing. docs/threat-model.md updated to reflect the implementation.
- N-gram code-text duplicate detector per spec section 13. New scripts/check-ngrams.mjs walks project text files (md/html/css/js/mjs/json/txt), tokenizes Unicode-letter+digit runs, and SHA-256-hashes every 25-token span (configurable via NGRAM_SIZE), comparing against a private fingerprint list at scripts/banned-ngrams.json (gitignored). Skips gracefully when the list is absent (the public-repo default), so the public CI run is non-blocking; environments with the list mounted at the expected path (or NGRAM_LIST env var) run the full scan and fail the build on any verbatim or near-verbatim 25-word match against NEC/IPC/IRC/IECC/IFC/IBC/ASHRAE/ACCA/NFPA/AWC published text. scripts/banned-ngrams.example.json documents the policy; npm run lint chains grep-checks and check-ngrams. Detector verified end-to-end against a planted 25-token fixture.
- Home-view payload trimmed under the spec section 11.1 100 KB budget. app.js now imports calculator modules (calc-electrical/plumbing/hvac/restoration/construction/fire/cross.js) and their support libs (hash-state.js, data-stamp.js, and transitively pure-math.js) via dynamic import() inside renderToolView; each module is fetched once on first tool open and cached by both the in-memory promise cache and the service worker. The footer-version stamp is deferred to requestIdleCallback so it does not delay FCP. Home-view files now total about 52 KB uncompressed (index.html + styles.css + app.js + integrity.js); calculator views load their module + support libs on demand. docs/performance.md updated to reflect the lazy-loading strategy. 229 unit tests still passing.
- Copy-all and live-region announcer per spec section 11.3. New clipboard.js exports copyText (Clipboard API + "Copied" announcement), addCopyAllButton (visible only when the output region has 2+ labeled rows; emits a labeled multi-line plaintext summary prefixed with the tool name), collectOutputs (label/value pair extraction from the output DOM), and announce. renderToolView wires addCopyAllButton generically so the affordance applies to all 64 utilities without per-calculator changes. A MutationObserver re-evaluates Copy-all visibility when the output region updates (e.g., when an example button populates or when the live result count changes). 4 new clipboard unit tests using a DOM stub, 233 total passing.
- Invalid-input behavior per spec section 11.2. New ui-validity.js delegates validity to HTML5 checkValidity() so existing min/max/step/required/pattern attributes drive the experience. On invalid: aria-invalid is set on the input, a brief plain-text reason (the browser's validationMessage) is rendered next to the field via a .validity-reason span, and the output region gains an .output-stale class that strikes through every output value via styles.css. Cleared once every input is valid again. wireValidity is loaded with the support libs and called from renderToolView, applying generically to all 64 utilities without per-calculator changes. 4 new ui-validity unit tests using a DOM stub with checkValidity stubs, 237 total passing.
- data-stamp.js test coverage. 8 new tests using fetch + DOM stubs verifying fetchManifest caches by folder and returns null on failure (no cache poisoning), stampDataSource emits the exact "Source: <label>, version <X>, fetched <Y>." format from the manifest, falls back to the manifest's per-shard `name` when no caller label is supplied, gracefully reports "manifest unavailable" when the fetch fails, and stampFooterVersion prefers build-info.json's timestamp, falls back to the physical-constants manifest version, and writes "Data version: dev" when nothing is available. 245 total passing.
- Routing helpers extracted to routing.js (parseHashRoute, decodePinnedList, toolMatches) so they can be unit-tested without a DOM stub. app.js delegates parseHash and matchesFilters to the pure helpers, eliminating duplication. 17 new routing unit tests covering the spec section 11.5 hash format invariants: empty/home/known/unknown tool ids, calculator state params, pinned format with whitespace and unknown-id filtering, defaulting on bad input; trade and group filter matching including multi-trade tools; case-insensitive query search across name and description. 262 total passing.
- Form-field helpers centralized into ui-fields.js (DEBOUNCE_MS, debounce, makeNumber, makeText, makeSelect, makeCheckbox, makeOutputLine, attachExampleButton, fmt). All seven calc-*.js modules removed their local copies and import from the shared module; the per-output Copy button now goes through clipboard.copyText so every Copy emits the "Copied" announcement consistently. 44 duplicate helper definitions deleted; calc bundle dropped from ~199 KB to ~182 KB plus a single 3.7 KB ui-fields.js shared on first calc-view load. 262 unit tests still passing; lint clean.

## v0.3.0 - 2026-05-06

The v3 expansion (the "Dirty Jobs Pack"). Tool count grows from one hundred nineteen to one hundred seventy-five visible tiles, plus four new platform affordances (Big Buttons mode, High-Contrast theme, Field Notes scratchpad, Crash-Safe Resume). 1344 unit tests passing across v1 + v2 + v3, lint clean, data:verify clean (63 entries / 54 shards). Home-view payload 33.1% of the 100 KB budget; every new calc module dynamic-imports on first use.

Highlights:

- Group A v3 (Electrical, utilities 125-131): pulling tension (capstan), cable bend radius, PF correction, phase balance, multi-load voltage drop, low-voltage DC drop, PoE budget.
- Group B v3 (Plumbing, utilities 132-138): stormwater rational method, Manning slope, hydrostatic test, grease trap, glycol mix, hydronic expansion tank, backflow loss.
- Group C v3 (HVAC, utilities 139-144): fan affinity laws, V-belt and pulley, compressed-air receiver, geothermal loop, hydronic baseboard output, NPSHa.
- Group D v3 (Restoration, utilities 145-146): containment air balance, drying chamber turnover.
- Group E v3 (Construction, utilities 147-158): drywall sheet count, roofing squares, asphalt tonnage, aggregate, mortar mix, ACI 211 simplified mix design, bolt torque, sheet-metal bend allowance, shop speeds and feeds, weld usage, demo debris, ACI 347 formwork pressure.
- Group F v3 (Fire-Ground, utilities 159-161): confined space air change, rope rescue MA, sling angle.
- Group G v3 (Cross-Trade, utilities 162-173): OSHA trench sloping, NIOSH lifting, heat stress, wind chill, ladder angle, pulley MA, ramp slope, rainwater yield, daily timesheet, vehicle load, plus two meta-utilities (Job Estimate Roll-Up and Material Order List) that compose existing session state.
- Group H v3 (References, utilities 174-179): hand signals, OSHA Top-10, lockout/tagout, defensible space, FEMA P-320 storm shelter, START triage with hardened "not medical advice" notice.
- Group I v3 (Platform): Job Pack (utility 180) - a printable job sheet over recents and bundled inputs; Big Buttons mode (utility 182) bumping every interactive element to 64 px; High-Contrast theme (utility 183) with WCAG AAA pure-black/pure-white pairings and a yellow focus ring; Field Notes scratchpad (utility 185) bound to the URL hash key `k=`; Crash-Safe Resume (utility 187) wrapping every renderer in a try/catch boundary that preserves the URL hash on failure.
- 17 new data shards in `data/electrical/`, `data/plumbing/`, `data/hvac/`, `data/construction/`, `data/crosswalks/`, and `data/summaries/`. All source values are public domain, manufacturer-attributed, or original creative work; no licensed code text is reproduced.
- 25 new derivation sections in docs/derivations.md covering capstan, PoE, PF correction, multi-load VD, rational method, Manning slope, expansion tank, hydrostatic test, affinity laws, V-belt length, air receiver, NPSHa, containment orifice flow, ACI 211 mix design, bolt torque, bend allowance, SFM-to-RPM, AWS deposition, ACI 347 formwork, capstan-and-pulley MA with friction, sling angle, NIOSH lifting, NWS heat index / wind chill, vehicle axle balance.
- Brand and UX polish: tab title trimmed to "Rough Logic" (the visible tagline now reads "tools for the trade."), section headers drop the "Group A / B / C" prefix in favor of just the category name, search bar gets native datalist autocomplete that routes straight to the matched tool.

Deferred to v3.x: utility 181 (QR of current URL - needs a bundled MIT QR library that has to be dynamic-imported to stay under the home-payload budget), utility 184 (numpad layout toggle - `inputmode="decimal"` already does the right thing), utility 186 (Send To Other Device - depends on utility 181). All three will land in a follow-on point release.

## v0.2.0 - 2026-05-05

The v2 expansion. Tool count grows from sixty-four to one hundred nineteen, plus six cross-cutting platform affordances. Every utility ships under the same v1 envelope: same-origin static assets, deterministic compute, no telemetry, no LLM, no third-party fetches at runtime, no licensed code text bundled. 802 unit tests passing across the v1 + v2 surface at the v0.2.0 cut (836 after subsequent unreleased polish), lint and data:verify green, 46 manifest hashes verified at boot.

Highlights:

- Group A v2 (utilities 65-71): Service Load Calculation (Residential), Generator Sizing, Solar PV String Sizing, Battery Runtime (with Peukert), Voltage Imbalance, GFCI/AFCI Reference, Lighting Power Density.
- Group B v2 (utilities 72-78): Water Hammer Arrestor (PDI WH-201), Hot Water Recirc Pump Head, Septic Tank Sizing, Trap Arm Length, Pipe Thermal Expansion, Tankless Water Heater GPM, Gas Leak Rate (orifice).
- Group C v2 (utilities 79-85): Refrigerant Charge Weighing, Approach and Delta-T Diagnostics, Outdoor Air Mix, Equivalent Length of Fittings, Wet-Bulb Sling Psychrometer, Pipe Insulation Thickness, Latent Heat Evaporative Cooling.
- Group D v2 (utilities 86-89): Standing Water Volume, NAM Sizing, HEPA Scrubber Filter Life, Thermal Imager Delta-T Reference.
- Group E v2 (utilities 90-99): Stair Stringer Length, Joist Mid-Span Deflection, Footing Area for Soil Bearing, Tile Count and Grout Volume, Paint Coverage, Excavation Volume, Brick / CMU Count, Wind Velocity Pressure, Snow Load (ASCE 7), Anchor Bolt Embedment.
- Group F v2 (utilities 100-104): Reverse-Lay Tandem-Pump Friction, Sprinkler GPM Density, Standpipe Friction Loss, Ladder Pipe Reach, Vehicle Braking Distance.
- Group G v2 (utilities 105-113): Loan Payment, Upgrade ROI / Payback, Mileage and Fuel Cost (IRS rate), Overtime Hours, Per-Diem (GSA), Geometry Pack, Dilution / Mixing Ratio, Slope from Digital Level, GPS Distance (haversine).
- Group H new (utilities 114-118): Wire / Pipe / Gas Color Codes, Knot Reference, Inspection Prep Checklist, Utility Locator and Emergency Contacts, Tool Maintenance Intervals.
- Group I cross-cutting (utilities 119-124): Compare Refrigerants (Group C side-by-side P-T view), Recent Tools, Project Bundle (URL + JSON), Print / PDF view, Offline status indicator, Example deep-link and Copy share link. All shipped.

Data: 13 new shards across electrical (demand-factors, lighting-density), plumbing (material-expansion, septic-rules), hvac (charge-per-foot, equivalent-lengths, insulation), restoration (hepa-loading), construction (soil-bearing, wind-snow-zones), crosswalks (irs-mileage, gsa-perdiem), summaries (v2-references). 46 manifest hashes verified at boot. Every shard cited in docs/data-sources.md and scripts/sources.md.

Derivations: 14 new sections in docs/derivations.md (PV string sizing, battery Peukert, pipe thermal expansion, OA mixing, cylindrical conduction, joist deflection, excavation frustum, wind velocity pressure, snow load, anchor bolt embedment, tandem-pump friction, vehicle braking, haversine + bearing).

Architecture: Home-view payload remains under the 100 KB budget. All v2 calc-*.js modules continue to be dynamic-imported on first tool open. New modules: calc-references.js (Group H), bundle.js (utility 121).

Operations: No A/B testing, no flags, no tracking, no email capture, no notifications. Same-origin Blob URLs for the Bundle Download action. CSP unchanged.

## v0.1.0 - 2026-05-04

Initial complete build of roughlogic.com per spec.md, sections 1 through 16. All 22 numbered build steps in spec section 14 implemented and verified locally. 220 unit tests passing across pure-math, all six trade groups, the cross-trade group, and the first-principles verification suite. Lint and grep checks green. Data integrity green across 33 manifest and shard hashes. Build produces 53 files / ~290 KB total in dist/. Ready for first deploy to Cloudflare Pages; the launch checklist in docs/launch-checklist.md enumerates the deploy-time gates that remain.

### Build progress

- Step 1 complete: repository scaffolding per spec.md section 6.
- Step 2 complete: documentation skeleton in docs/.
- Step 3 complete: README per spec.md section 14 step 3.
- Step 4 complete: visual skeleton in index.html and styles.css with CSP meta tag, header, search box, trade and group filters, tile grid, footer disclaimer.
- Step 5 complete: hash-based routing in app.js, tool registry of all 64 utilities, trade and group filters, search, leader-key shortcuts (G prefix) and `?` overlay, ESLint config and CI grep checks (innerHTML, eval, new Function, emoji, em-dash).
- Step 6 complete: service worker (sw.js) with cache-first shell and data-shard cache, build-hash-keyed cache name, navigation fallback to index.html when offline. App registers SW only on https or localhost.
- Step 7 complete: scripts/build-data.mjs produces 24 shards across 9 datasets (physical-constants, electrical, plumbing, hvac, restoration, construction, fire, crosswalks, summaries) with per-folder manifest.json and SHA-256 hashes; scripts/verify-integrity.mjs checks every shard against scripts/expected-hashes.json; scripts/sources.md records provenance.
- Step 8 complete: pure-math.js library implementing AWG geometry, conductor resistance at temperature, ampacity from heat balance, voltage drop, single- and three-phase power, Hazen-Williams, Darcy-Weisbach with Colebrook iteration, beam mechanics, allowable spans by bending and deflection, psychrometrics (August-Roche-Magnus), refrigerant P-T linear interpolation, fire hose friction (CQ^2L), hydrant flow, temperature conversions. 41 unit tests passing on the Node built-in runner.
- Step 9 complete: Group A Electrical (utilities 1 through 11) in calc-electrical.js. Live-rendered calculator views with Test-with-example buttons, Copy affordances, inline citations, and aria-live output regions for Ohm's Law, Wire Ampacity, Voltage Drop, Conduit Fill, Box Fill, Breaker Sizing, Motor FLA, Transformer Sizing, Three-Phase Power, Conductor Resistance at Temperature, EGC Sizing. Renderers wired into app.js. 23 additional unit tests, 75 total passing.
- Step 10 complete: Group B Plumbing and Gas (utilities 12 through 20) in calc-plumbing.js. Hunter's-Curve fixture-unit pipe sizing, Hazen-Williams and Darcy-Weisbach friction loss, pipe volume, hydraulic-hp pump sizing, static pressure with elevation, Spitzglass gas pipe sizing, slope, NIST SP 811-based pressure conversion, backflow reference. 23 additional unit tests, 98 total passing.
- Step 11 complete: Group C HVAC (utilities 21 through 31) in calc-hvac.js. Simplified Manual J cooling and heating estimators, Darcy-Weisbach duct sizing with Colebrook iteration, static pressure summation, refrigerant P-T chart for R-410A/R-32/R-22/R-134a/R-404A/R-407C with manufacturer attribution, superheat and subcool, SEER/EER conversion, heat-pump balance point, SHR, CFM per ton, combustion air. Manual J cooling and heating and duct sizing run in manual-j-worker.js Web Worker (with in-thread fallback for tests). 27 additional unit tests, 125 total passing.
- Step 12 complete: Group D Restoration (utilities 32 through 39) in calc-restoration.js. Psychrometric calculator (dew point, GPP, vapor pressure), drying goal (target GPP and indoor RH), AHAM and field-method dehumidifier sizing, air mover placement by water class, IICRC water classes/categories reference, material drying times reference, mold growth risk, PPE selection by water category. 19 additional unit tests, 144 total passing.
- Step 13 complete: Group E Carpentry and Construction (utilities 40 through 50) in calc-construction.js. Stair geometry, roof pitch, rafter length, square footage (rectangle/triangle/trapezoid/circle), board footage, concrete volume (slab/footing/column/footing-with-stem), rebar quantity, lumber spans (first-principles beam mechanics with bundled species/grade properties), nail/screw withdrawal capacity (G^2.5 * D * 1380), beam loading (uniform and centered point), material quantity for common assemblies. 28 additional unit tests, 172 total passing.
- Step 14 complete: Group F Fire-Ground Engineering (utilities 51 through 58) in calc-fire.js. CQ^2L hose friction with NFA coefficients (1.5/1.75/2.5/3/4/5 in), pump discharge pressure (with 0.5 psi/ft elevation), hydrant flow, ISO needed-fire-flow estimator, master-stream reach, aerial-ladder reach, foam concentrate volume, smoke reading reference. 17 additional unit tests, 189 total passing.
- Step 15 complete: Group G Cross-Trade (utilities 59 through 64) in calc-cross.js. Comprehensive unit converter (length, area, volume, mass, force, pressure, energy, power, flow, electrical voltage/current/resistance/capacitance, temperature with affine handling), material cost estimator with optional tax and delivery, markup/margin calculator (bidirectional), time and materials estimator with overhead and profit, sales tax with bundled state rates and override, tip-out splitter. 24 additional unit tests, 213 total passing.
- Step 16 complete: Lighthouse CI configured at lighthouserc.json with FCP < 1s, LCP < 1.5s, TBT < 100ms, CLS < 0.05, score >= 95 across performance/accessibility/best-practices/SEO; runs in .github/workflows/ci.yml against home view plus three representative utility views. docs/performance.md documents the budget. .github/workflows/data-refresh.yml schedules the monthly data pipeline and opens a PR if shards change.
- Step 17 complete: Playwright + axe-core integration tests at test/integration/a11y.test.js covering WCAG 2.2 AA across 16 representative views, single-h1 invariant, focused h1 on tool views, aria-pressed toggling on filter buttons, and 48x48 minimum touch targets. test/integration/playwright.config.js spawns a same-origin static server. CI-only dev dependency; npm run test:e2e and test:a11y wired to Playwright. index.html title and theme-color meta added.
- Step 18 complete: _headers file at the repository root carries CSP (default/script/style/img/connect/form-action/base-uri 'self', frame-ancestors 'none', object-src 'none', worker-src 'self'), HSTS one-year preload, X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy no-referrer, COOP same-origin, COEP require-corp, CORP same-origin, Permissions-Policy disabling camera/microphone/geolocation/payment/USB/accelerometer/gyroscope/magnetometer. CSP meta tag in index.html updated to match. robots.txt, sitemap.xml, and site.webmanifest added.
- Step 19 complete: First-principles verification suite at test/unit/first-principles.test.js cross-checks copper resistance per kft (within 5% of published DC values), ampacity at 75 C / 30 C ambient (within 30% of NEC 75 C column), Hazen-Williams worked example, refrigerant P-T at bundled table points, lumber spans (within 20% of published-equivalent values for representative joists), NFA hose friction, and hydrant flow. CI workflow at .github/workflows/ci.yml runs lint, unit, data:verify, Lighthouse, and accessibility on every push and PR; data-refresh workflow runs monthly. 220 unit tests passing.
- Step 20 complete: scripts/analyze-data-changes.mjs walks each per-folder manifest, diffs current shards against the previous git revision, and emits a Markdown summary covering changed/added/removed shards with old/new hashes plus per-shard added/modified/removed entries (with drill-down into values/rates/refrigerants/summaries layers). Wired into the data-refresh workflow as the PR body. Footer Changelog link added to index.html.
- Step 21 complete: scripts/build.mjs copies the static shell, all calculator modules, _headers, robots.txt, sitemap.xml, site.webmanifest, CHANGELOG, LICENSE, and the data/ tree into dist/, plus a build-info.json with the build timestamp. scripts/dev.mjs is a Node-only static server with the production CSP applied for local development. npm run dev / build / clean wired. docs/deployment.md documents the Cloudflare Pages configuration. Build output: 53 files, ~290 KB.
- Step 22 complete: docs/launch-checklist.md is the written report per spec section 14 step 22. 19 items pass locally with automated assertions; 11 items are gated on first-deploy verification (Mozilla Observatory A+, securityheaders.com A+, HSTS preload submission, CSP enforcement in console, deployed offline behavior, deployed Lighthouse run). 220 unit tests passing, lint clean, data-integrity passing.
