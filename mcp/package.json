{
  "name": "roughlogic-mcp",
  "version": "0.401.1",
  "private": true,
  "description": "Local MCP server exposing the roughlogic trades calculators (search / describe / run). Runs from a checkout of this repo, which it reads directly; it is not a publishable package.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "roughlogic-mcp": "./server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
